# Verified Tasks: T-0390 · T-0418 · T-0391 — SP-100, EP-031 verification & Epic close

**Status:** ✅ **Implemented - Verified**
**Sprint:** SP-100 (✅ Closed 2026-08-19, user-approved) — EP-031's **eleventh and last**
**Epic:** EP-031 `[ScriviCore]` Worldbuilding Object Model & Relationship Graph
**Date Verified:** **2026-08-19 (user-approved)**

---

## T-0390 — External Change Repair Matrix: world-package conditions

**`Scrivi_External_Change_Repair_Matrix_v0_2.md`: 578 → 826 lines. World mentions: 0 → 72.**

⚠️ **Before this task the matrix contained ZERO occurrences of "world" or "scrivworld"** across 578 lines
and 21 conditions — worlds landed in SP-097, three sprints after its last revision. **A repair pass
written against the document as it stood could have destroyed a writer's entire relationship graph and
satisfied every rule in it.**

New **§6a**, seven conditions in the matrix's established format, each **asserted against shipped code**:

| § | Condition | Backed by |
| - | --------- | --------- |
| **6a.0** | ⚠️ **The governing principle: ABSENCE IS NEVER DELETION** | — |
| 6a.1 | World package missing entirely | `WorldTests.cpp:218,234,277` |
| 6a.2 | ⚠️ `worldID` mismatch — a same-named package | `WorldTests.cpp:194,306` |
| 6a.3 | `world.json` corrupt or unparseable | ⚠️ gap → I-0135 |
| 6a.4 | Binding exists, world permanently unresolvable | `ObjectCApiTests.cpp:420,436,493,518,555` |
| 6a.5 | Stale write lock | `WorldTests.cpp:108,356,376,405` |
| 6a.6 | Object present but absent from the world index | `ObjectIndexTests.cpp:277,295,323,363,390` |
| 6a.7 | Worldless project — ⚠️ **an expected state, not a defect** | `WorldTests.cpp:338` · `ObjectCApiTests.cpp:580` |

§5's state vocabulary gained `world unavailable` · `world missing` · `world identity mismatch` ·
`world locked`, with the rule that ⚠️ **`unavailable` and `missing` must never be collapsed.**

✅ **All 23 test citations were verified to resolve** to real `TEST_CASE`s with matching descriptions — a
citation that did not resolve would be the very defect §6a documents.

**Filed, not fixed (R3):** **I-0135**, **I-0136**.

---

## T-0418 — ⚠️ Live-use pass on the real USB rig

**Run by the user 2026-08-19** on `~/Desktop/the-stairs-of-tintagael.scrivi` bound to
`/Volumes/Scrivi Worlds/Eskandar.scrivworld`, backed up beforehand.

| Step | Result |
| ---- | ------ |
| 1. Create in all 10 world kinds | ✅ **PASS** — ⚠️ `source` not created, **no UI exists** (EP-034) |
| 2. Relate from both entrances | ⛔️ **BLOCKED** by I-0139 |
| 3. Eject | ✅ **PASS** — warnings, named pending rows, disabled removal. Found I-0137, I-0138 |
| 4. **Reattach** | ✅ **PASS** — ⚠️ **AC23's no-intervention clause HELD** |
| 5. Quit / reopen ×2 | ✅ **PASS** |

⚠️ **Step 4 is the clause no fixture could establish.** It is **I-0129's exact defect** — refresh keyed to
app focus rather than the mount event — and it held: reattaching restored every card **with no click, no
menu, no relaunch.**

**Step 1 is AC1's re-verification performed by use.** ⚠️ Four kind directories (`buildings`, `items`,
`maps`, `vehicles`) were **absent beforehand** — the world predates SP-104's skeleton fix by one day —
**and were created on demand.** The pre-SP-104 gap is **cosmetic, not blocking**, and this world was
stronger evidence than a fresh one.

**Filed, not fixed (R4):** **I-0137** (High), **I-0138**, **I-0139**.

---

## T-0391 — EP-031 verification (AC1–AC10) + Epic close prep

**All ten ACs verified; AC1 and AC10 ticked by the user 2026-08-19.**

**Suites at close:** `ctest` **520/520 macOS arm64** · x86-64 + sanitizers ✅ (CI 2×2 — ⚠️ **ScriviCore
verified byte-identical** to that run, so the evidence is current, not stale) · macOS interop **99/99 in
10 suites** · app **BUILD SUCCEEDED**.

**Also delivered:**
- **Doc 1 §3.0 consequence 4 amended** — AC1 was *amended then re-verified*, with the reasoning recorded.
- ✅ **T-0369 resolved** — from the archives, **without a user ruling**, because `Epic-EP-030.md` states
  all seven ACs Verified with a dated per-AC table. ⚠️ **The record was missing, not the verification.**
- **Two stale rows corrected** in `Epic-active.md` (SP-102 shown Active; T-0389 shown Not Verified).
- ✅ **Audit Check run** (exit criterion 7) — all seven mechanical checks pass.

### ⚠️ The finding that outlives this Epic

**I-0137: AC24's `unmounted`/`offline` refinement cannot fire on real hardware.**
`WorldVolumeStatus.refine` is correct, unit-tested and correctly wired — and it guards on
`packagePath.isEmpty`, while `WorldStore::listWorlds` supplies `packagePath` **only when the world is
`available`**. ⚠️ **The one input it needs is guaranteed absent in the only case it exists for.**

⚠️ **The interop suite contains a suite named *"World volume status refinement (EP-031 AC24)"* — and it
passes.** That is not a contradiction; it is the shape of the defect. **What no unit test can see is that
the caller never supplies the input.** *The tests pass and the feature does not reach the writer.*

**User ruling 2026-08-19:** *"The Task was verified. What was not is due to unimplemented software
features."* — **T-0389's verification stands**; the defect is in a component it does not own.

---

*Verified 2026-08-19 (user-approved). SP-100 closed the same day, closing EP-031.*
