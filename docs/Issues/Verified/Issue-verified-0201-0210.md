# Verified Issues I-0201 – I-0210

Issues the **user has explicitly verified**. Archived out of `Issue-active.md` in the same step
they were verified (per `Issue-GUIDELINES.md`).

| ID | Title | Severity | Sprint | Status |
| -- | ----- | -------- | ------ | ------ |
| **I-0203** | `[Apple]` ⚠️ **THE SCENE NAVIGATOR STOPS RENDERING — the writer cannot see it.** ⚠️ **Reported by the USER 2026-09-12:** *"the Scene Navigator hid itself and I couldn't see it."* ⚠️ **SAME defect as the earlier report that "the window, when not maximized, does not render the Navigation Bar."** ---- ✅ **TRIGGER FOUND 2026-09-14 — AND IT IS NOT MAIN-THREAD STARVATION.** ⚠️ **THE PREVIOUS HYPOTHESIS IS DISPROVEN, NOT MERELY UNCONFIRMED.** ⚠️ **Both the user and I believed the UI was so unresponsive that AppKit stopped servicing layout.** ✅ **[I-0204] made the main thread RESPONSIVE (the 33 s `buildClusters` stall is gone, `NSClickGestureRecognizer … possible phase` no longer appears at all) — ⚠️ AND THE CHROME STILL DID NOT RENDER.** ✅ **Starvation is therefore ELIMINATED as the cause.** ---- ✅ **THE ACTUAL TRIGGER, USER-OBSERVED AND REPRODUCIBLE: the `"1 world is unavailable"` WARNING BANNER.** ⚠️ **While the banner is present: the Nav Bar is ABSENT, the Scene Inspector's TAB BAR is ABSENT, the TIMELINE is ABSENT, and a stray title panel reading `"The France of Alexandre Dumas"` sits ATOP the manuscript content, with manuscript text visible all the way to the top of the window.** ✅ **DISMISSING THE BANNER (clicking its X) FIXES ALL FOUR AT ONCE:** ✅ **(1) the stray title panel MERGES INTO the Nav Bar; (2) the Nav Bar appears; (3) the Inspector Tab Bar appears; (4) the Timeline appears.** ⚠️ **Four elements restored by ONE dismissal is a LAYOUT/HIERARCHY fault — the banner is DISPLACING the window chrome — not four independent defects.** ✅ **`_NSDetectedLayoutRecursion` still firing is CONSISTENT with this and no longer needs a starvation explanation.** ---- ⚠️ **RESIDUE AFTER DISMISSAL (likely the same fault, tracked here): a TALL TRANSLUCENT RECTANGLE, ~1.5× the height of the Nav Bar, appears BELOW the Nav Bar**, ⚠️ **obscuring manuscript text at the top of the viewport and the top of the Scene Inspector**, ⚠️ **spanning from the LEFT edge of the Manuscript View to the RIGHT edge of the Scene Inspector.** ✅ **PRESENT ONLY WHEN NOT IN FULL SCREEN.** ⚠️ **Its identity is UNKNOWN — it must be identified in the view hierarchy, not guessed.** ---- ⚠️ **MY EARLIER `NavigationSplitView` SIZING HYPOTHESIS REMAINS REJECTED** (`EditorView.swift:201`, `.balanced`, no `navigationSplitViewColumnWidth`, `columnVisibility` wired only to the iOS branch at `:161`). ✅ **Still worth tidying**, ⚠️ **but it does not explain a banner-gated trigger.** ⚠️ **NOT INTRODUCED BY [SP-132]** — ✅ **`SceneNavigatorView.swift` is UNMODIFIED from HEAD** (verified), ⚠️ **and the first report predates that sprint.** ---- ⚠️ **WHY IT MATTERS: the navigator is the ONLY way to move between 1,156 scenes.** ⚠️ **A writer who loses it loses navigation entirely, with no affordance to bring it back — ✅ and the ONE affordance that restores it (dismissing an unrelated warning) is undiscoverable.** ---- ✅ **CODE READ 2026-09-14 (first time — the record previously carried NO code reading).** ✅ **FOUR STRUCTURAL FACTS, ESTABLISHED:** ✅ **(1) THE BANNER IS A SIBLING OF THE MANUSCRIPT INSIDE A `VStack`** (`EditorView.swift:283-302`): `ManuscriptTextView` carries `.frame(maxWidth: .infinity, maxHeight: .infinity)` and the banner is appended BELOW it in the same `VStack`. ⚠️ **I HYPOTHESISED a SwiftUI over-commit here (greedy sibling squeezes the chrome) — ✅ **THE VIEW HIERARCHY DISPROVED IT** (see CAPTURE below): the chrome is STRUCTURALLY INTACT in both states.** ✅ **(2) `manuscriptDetail` — the view the banner lives inside — IS ALSO THE VIEW CARRYING `.navigationTitle` / `.navigationSubtitle`** (`:229-230`). ⚠️ **That is EXACTLY the pairing the user observed: the stray *"The France of Alexandre Dumas"* panel is the NAVIGATION TITLE rendering as a separate band, and dismissing the banner lets it MERGE BACK into the window title area.** ✅ **(3) THE macOS EDITOR HAS NO TOOLBAR AT ALL.** ⚠️ **`.toolbar { editorMenuToolbar }` is attached ONLY on the iOS branch (`:189`), and `editorMenuToolbar` is itself inside `#if os(iOS)` (`:483-504`).** ⚠️ **This is CORRECT and deliberate — macOS uses the real menu bar — ✅ but it means the thing the user calls the *"Nav Bar"* is the WINDOW TITLE AREA, not a toolbar, so any fix aimed at a toolbar would be aimed at nothing.** ✅ **(4) THE WINDOW IS A BARE `NSHostingView` IN A `.titled` WINDOW** (`ProjectWindowManager.swift:89-93`) — ⚠️ **no `NSToolbar`, no `titlebarAppearsTransparent`, no split-view controller.** ✅ **So SwiftUI's title/subtitle have no AppKit toolbar to render into, which is why they surface as an in-content band.** ---- ⚠️ **THIS IS A HYPOTHESIS WITH EVIDENCE, NOT A PROVEN CAUSE.** ⚠️ **It explains all five observations (missing title area, missing inspector tabs, missing timeline, the stray title panel, and all of them returning together on dismissal) — ✅ but it has NOT been confirmed against the running view hierarchy.** ⚠️ **THREE CODE-READ DIAGNOSES WERE WRONG EARLIER IN THIS SAME INVESTIGATION** ([I-0204]'s cause was found by `sample`, not by reading) — ⚠️ **so this must be CONFIRMED by inspecting the live hierarchy (Xcode's Debug View Hierarchy with the banner up) BEFORE any fix is written.** ⚠️ **The translucent rectangle remains UNIDENTIFIED and is the specific thing that inspection must name.** ---- ✅ **VIEW HIERARCHY CAPTURED 2026-09-14 (user-supplied, both states) — THIS SUPERSEDES THE CODE-READ HYPOTHESIS ABOVE.** ⚠️ **TWO OF MY READINGS WERE WRONG AND ARE RECORDED AS SUCH:** ⚠️ **(i) the `VStack` over-commit — ✅ DISPROVEN: `NSToolbarView`, `NSTitlebarView` and the widgets are PRESENT AND IDENTICAL in both captures; nothing is squeezed out of the layout;** ⚠️ **(ii) `TUINSWindow` as the translucent rectangle — ✅ DISPROVEN: it is present in BOTH states (it was merely omitted from the first dismissed capture).** ---- ✅ **THE ONLY REAL STRUCTURAL DIFFERENCE, inside `NSTitlebarContainerView`:** ✅ **BANNER SHOWN → TWO `NSScrollViewMirrorView` + `NSPortalView` + `_NSPortalLayerBackedView` triples.** ✅ **BANNER DISMISSED → ONE triple, ⚠️ PLUS an `NSScrollPocket`.** ---- ✅ **THE TRANSLUCENT RECTANGLE IS `NSScrollPocket`** (user-identified), ⚠️ **and it does NOT live in the titlebar branch at all:** `NSSplitViewController → NSView → NSSplitView → `_NSSplitViewItemViewWrapper` → **`NSTitlebarBackgroundView`** → **`NSScrollPocket`**. ✅ **That placement — INSIDE the split-view item — is exactly why it spans from the manuscript's left edge to the Scene Inspector's right edge and obscures the top of BOTH**, ⚠️ **and why it is ABSENT IN FULL SCREEN (no titlebar to inset under).** ---- ✅ **MECHANISM (one cause, both symptoms): AppKit's TITLEBAR SCROLL-EDGE INTEGRATION is binding to the WRONG scroll view, and RE-BINDS when the banner changes the subview set.** ⚠️ **A mirror triple is a live portal of a scroll view painted BEHIND the titlebar; TWO triples means AppKit is mirroring TWO scroll views into ONE titlebar** — ✅ **which is the title area rendering as a floating band over content rather than as chrome.** ✅ **`NSScrollPocket` is the companion inset AppKit carves so scrolled content can slide under the titlebar.** ⚠️ **So this is NOT a SwiftUI sizing fault and NOT the banner's own layout — it is automatic AppKit chrome/scroll integration resolving an AMBIGUOUS target.** ---- ⚠️ **WHAT IS STILL NOT ESTABLISHED: WHICH scroll views are being mirrored, and WHY the set is ambiguous.** ⚠️ **The editor contains at least the manuscript `NSScrollView` (`ManuscriptTextView.swift:126`) and the navigator's `List`; ✅ the next step is to identify the two mirrored targets by ADDRESS in a capture, not by inference.** ⚠️ **DO NOT 'FIX' THIS BY RESTYLING THE BANNER — the banner is only the trigger that changes the subview set.**  ---- ✅ **CONFORMANCE STUDY WRITTEN 2026-09-14** → [`../Scrivi_Apple_UI_Conformance_Trade_Study_v0_1.md`](../Scrivi_Apple_UI_Conformance_Trade_Study_v0_1.md) (⚠️ **DRAFT, awaiting ruling**). ✅ **It SOURCES the mechanism from Apple:** ✅ **the scroll edge effect is applied AUTOMATICALLY beneath toolbar items, titlebar accessories and split item accessories** ([WWDC25 §310](https://developer.apple.com/videos/play/wwdc2025/310/)), ⚠️ **and macOS 27 resolves `automatic` to the HARD style WHEN FREE-FLOATING TITLE TEXT IS PRESENT** ([WWDC26 §289](https://developer.apple.com/videos/play/wwdc2026/289/)) — ✅ **which is Scrivi's exact case (`.navigationTitle` with NO `NSToolbar` anywhere in the app).** ✅ **F3: every bar here is a plain `VStack` SIBLING — banner, Inspector tab bar AND Timeline all share one `VStack` — so none of them insets the safe area or extends the edge effect**, ⚠️ **which is why ONE dismissal moves all four.** ⚠️ **THE STUDY DOES NOT UPGRADE THIS RECORD:** ⛔ **the two mirrored scroll views are now identified STRUCTURALLY (the navigator `List` at `SceneNavigatorView.swift:104` and the manuscript `NSScrollView` at `ManuscriptTextView.swift:126`) but STILL NOT BY ADDRESS** — ⚠️ **that capture remains REQUIRED before any fix.** | **Medium** | ✅ **[EP-040]** `[Apple]` **The Editor Shell** (🔵 **DRAFT**) — ✅ **AC5 is this Issue's acceptance test**; ⚠️ **fixed by S3 (`safeAreaBar`), SP-135.** ⚠️ **NOT scheduled — awaiting [SP-133]** (⚠️ **that gate assumed starvation, which is now disproven**) | 🟢 **RESOLVED - Not Verified (2026-09-22, [SP-135]/T-0545).** ✅ **THE HIERARCHY WAS READ, as this record demanded, and it WAS the cause it predicted.** ⚠️ **ROOT CAUSE: the banner, the Timeline and the Inspector tab bar were all `VStack` SIBLINGS of the content** (`EditorView.swift`, `SceneInspectorView.swift`). ⛔ **A sibling TAKES vertical space from the stack it joins** — ✅ **so the banner appearing pushed on everything sharing that stack at once**, ⚠️ **which is why ONE structural mistake read as FOUR bugs (Nav Bar, Inspector tab bar, Timeline and a stray title panel all moving together).** ✅ **FIX: all three converted to `safeAreaBar`** — ⚠️ **a safe-area bar INSETS the content instead of displacing its neighbours.** ✅ **USER LIVE PASS 2026-09-22 — BOTH HALVES, CHECKED SEPARATELY:** ⚠️ ***"The banners all appeared in the correct places and did not deform the UI"*** · ⚠️ ***"I closed the banner and only the timeline adjusted its size."*** ✅ **THAT SECOND RESULT IS THE PROOF: the Timeline is the INNER bar, so it reclaiming the freed inset is exactly correct** — ⛔ **and NOTHING above it moved.** ⚠️ **The stray title panel is separately gone** — ✅ **[I-0243]/T-0544 removed the title triplication.**  ---- ✅ **VERIFIED BY THE USER 2026-09-24.** ⚠️ **THE RECORD WAS STALE, NOT THE FIX:** ⚠️ **[SP-135]'s live pass on 2026-09-22 DID verify this — ✅ its closed record says so and [EP-040] has cited it as MET ever since** — ⛔ **but this row was never moved out of `Issue-active.md`, so the Issue layer and the Epic layer disagreed for two days.** ✅ **FOUND MECHANICALLY by the Audit Check run before [EP-040]'s close** (`docs/Audits/Audit-Check-20260924.md`, finding **F-3**), ⚠️ **exactly the failure shape that instrument exists to catch: an Issue cited as evidence for a MET AC with no archive entry.** ✅ **USER RULING 2026-09-24:** *"The records are stale. I-203 and I-205 are resolved."* |
| **I-0204** | `[Apple]` ⚠️ **`TimelineStripView.buildClusters` BURNS THE MAIN THREAD FOR ~33 SECONDS ON EVERY LAYOUT PASS — this is the real cause of [I-0200]'s post-load unusability.** ✅ **MEASURED, NOT READ** — `sample Scrivi` captured during a live stall, 2026-09-14 (`/tmp/scrivi-stall.txt`, 6,595 main-thread samples). ✅ **4,796 of 6,595 main-thread samples — 73% — are inside `buildClusters`.** ⚠️ **ZERO samples in the Scene Navigator; TextKit registers a handful.** ---- ✅ **THE STACK:** `CA::Transaction::flush` → `NSWindow layoutIfNeeded` → `TimelineStripView.body` (`:710`, `:762`) → `buildClusters` → `itemX` → `eventX` → `visibleSpanMs`/`visibleMinMs` → `effectiveZoom` → `maxZoom` → `smallestMainRowGapMs()` **+** `model.spanMs`. ---- ⚠️ **FOUR UNCACHED LAYERS, EACH O(N) OR O(N log N), NONE MEMOISED:** ⚠️ **(1) `TimelineViewModel.maxEndMs` (`:228`) maps ALL 1,156 dots; `minOffsetMs` (`:223`) maps them again; `spanMs` (`:232`) calls BOTH;** ⚠️ **(2) `smallestMainRowGapMs()` (`:640`) BUILDS AND SORTS a 1,156-element array on EVERY call;** ⚠️ **(3) `maxZoom` (`:629`) calls both of the above, and `effectiveZoom` (`:652`) calls `maxZoom`;** ⚠️ **(4) `eventX` (`:1299`) calls `visibleSpanMs()` AND `visibleMinMs()`, and EACH calls `effectiveZoom` — ✅ so ONE `itemX` triggers ~4 full sorts + ~6 full maps.** ---- ⚠️ **THE MULTIPLIER — `itemX` IS CALLED INSIDE A SORT COMPARATOR** (`:1390`): `allItems.sort { itemX($0…) < itemX($1…) }`. ✅ **n=1,156 → ~11,600 comparisons × 2 calls = ~23,000 `itemX` calls, each re-sorting 1,156 elements — ~27 MILLION element-operations per `buildClusters`, before the cluster loop even begins** (which calls `itemX` again per item). ---- ✅ **WHY THE STALL IS CONSTANT AT ~33 s:** ⚠️ **the cost depends ONLY on scene COUNT — never on which scene, its offset, or its index.** ✅ **THREE RUNS CONFIRM: offsets 4,962 / 44,649 / 60,657 → gesture stalled 32.70 s / 33.45 s / ~33 s.** ⚠️ **I repeatedly MISREAD that constancy as "the thread is WAITING" (I theorised an XPC/sandbox timeout); ✅ the sample proves it is PINNED AT 100% CPU, not blocked.** ---- ✅ **WHY EVERY `[SCRIVI-TIMING]` PROBE SAID "FAST" AND WAS HONEST:** ⚠️ **the timeline renders during the AppKit LAYOUT pass, OUTSIDE every instrumented bracket.** ✅ `WALL CLOCK 0.98 s` vs a user-observed **41 s** open; ✅ `[SCRIVI-NAV]`+`[SCRIVI-UPD]` total ~300 ms vs a user-observed **35 s** click. ⚠️ **A probe INSIDE a handler can never see a cost that runs BETWEEN handlers — this is the lesson, and it cost three rounds.** ---- ✅ **FIX (3 parts):** ✅ **(a) cache `minOffsetMs`/`maxEndMs`/`spanMs` on the model via `didSet` on `dots`+`historicalEvents`;** ✅ **(b) cache `smallestMainRowGapMs()` the same way;** ✅ **(c) decorate-sort-undecorate in `buildClusters` — compute each item's x ONCE, sort on the stored value (~23,000 `itemX` calls → 1,156).** | ⚠️ **HIGH** | ✅ **SP-133** (EP-039) | ✅ **VERIFIED 2026-09-14 (user-approved).** ✅ **CONFIRMED BY LIVE RUN + a SECOND INDEPENDENT `sample` (pid 51023): `buildClusters` fell from **4,796 of 6,595** main-thread samples to **5 of 7,150**.** ✅ **Evidence from the first run:** ⚠️ **the pass/fail signal — `NSClickGestureRecognizer … in possible phase for ~33 seconds` — is ABSENT FROM THE ENTIRE SESSION**, ✅ **across four navigator clicks, sustained typing, a chapter add and a scene add.** ✅ **Navigator click `35–45 s` → `~0.3 s`; the user reports the app is USABLE.** ⚠️ **`updateNSView` (`~100–160 ms`) and `guard` (`0.3–0.4 ms`) are UNCHANGED — ✅ confirming they were never the cost, as the sample said.** ✅ **USER-VERIFIED 2026-09-14.** |
| **I-0205** | `[Apple]` ⚠️ **THE `"1 world is unavailable"` WARNING BANNER IS SHOWN FOR A CONDITION THAT IS NORMAL, AND IT DISPLACES THE WINDOW CHROME WHILE SHOWN.** ⚠️ **Observed by the USER 2026-09-14 on the `dumas-prose` fixture.** ---- ⚠️ **TWO SEPARABLE COMPLAINTS, FILED TOGETHER BECAUSE ONE BANNER CAUSES BOTH:** ✅ **(1) THE LAYOUT DAMAGE is tracked in [I-0203]** (Nav Bar, Inspector tab bar and Timeline all absent while the banner is up; all four restored by dismissing it) — ⚠️ **not restated here.** ⚠️ **(2) THE BANNER'S EXISTENCE IS ITSELF QUESTIONABLE, and that is THIS record.** ---- ⚠️ **A world being unavailable is NOT NECESSARILY AN ERROR STATE.** ✅ **Per `project_unlinked_world_objects_are_normal`: a shared world's objects are unlinked in every new project BY DEFINITION, and an absent world is the EXPECTED condition when the volume holding it is simply not mounted.** ⚠️ **Presenting that as a persistent warning that must be manually dismissed — ✅ and which BREAKS THE WINDOW CHROME until it is — is disproportionate to the condition.** ---- ⚠️ **WHAT MUST BE ESTABLISHED BEFORE ANY FIX (do NOT skip to the fix):** ⚠️ **(a) WHICH world is unavailable and WHY** — ✅ **the fixture is on the Desktop, not on the removable rig drive, so a missing USB world is NOT the obvious explanation and must not be assumed;** ⚠️ **(b) whether the banner is CORRECT-BUT-UGLY (a real missing world, badly presented) or FALSE (a world that is present being reported absent).** ⚠️ **These lead to OPPOSITE fixes and the evidence does not yet distinguish them.** ---- ⚠️ **WHY IT MATTERS: ✅ if the banner is correct, a writer is being punished with a broken window for a normal condition; ⚠️ if it is false, a world-availability check is wrong and that is a DATA-facing defect, not a cosmetic one.**  ---- ✅ **CONFORMANCE STUDY 2026-09-14** → [`../Scrivi_Apple_UI_Conformance_Trade_Study_v0_1.md`](../Scrivi_Apple_UI_Conformance_Trade_Study_v0_1.md) §6. ⚠️ **THE STUDY EXPLICITLY DOES NOT ANSWER THIS RECORD** — ✅ **it explains the banner's LAYOUT DAMAGE (that is [I-0203]) and says NOTHING about whether the banner should exist.** ⚠️ **Questions (a) and (b) above STAND and must be answered from evidence before the banner is touched.** | **Medium** | ✅ **[EP-040]** — ⚠️ **AC9, and it GATES S3**: ✅ **this must be ANSWERED before the banner is restyled**, ⚠️ **or a false banner gets entrenched as a design** | 🟢 **DIAGNOSED AND RULED 2026-09-22 ([SP-135] / T-0545) — ⚠️ AWAITING USER VERIFICATION OF THE FIX.** ✅ **THE CODE HAS NOW BEEN READ, which this record required before anything could be cited.** ✅ **ANSWER: (a) — THE BANNER IS CORRECT, NOT FALSE.** ⛔ **Question (b) is CLOSED: no availability check is wrong, and the DATA-facing defect this record feared does not exist.** ⚠️ **EVIDENCE:** `WorldWarningView.swift:128` builds its rows as `worlds.filter { $0.worldStatus.isUnavailable }` — ✅ **it reports every unavailable BOUND world, DELIBERATELY**, and the code states why: ⚠️ ***"Report every unavailable BOUND world, not merely those with pending edges. A world that is away with nothing yet linked is still something the writer needs to know about — she cannot add to it, and silence would read as 'everything is fine'."*** ✅ **USER RULING (2026-09-22): KEEP THE BANNER.** ⛔ **NOT demoted to a Worlds-toolbar badge; ⛔ NOT limited to worlds with pending edges.** ⚠️ **THE TRADE IS RECORDED RATHER THAN HIDDEN: an unavailable world IS a normal condition** (`project_unlinked_world_objects_are_normal`) — ✅ **so this can be revisited from the FIXED layout; ⛔ a warning that was never shown cannot be.** ✅ **THE LAYOUT HALF IS FIXED AND VERIFIED:** ⚠️ **[I-0203]'s displacement is gone** — ✅ **the user's live pass 2026-09-22 confirmed the banner appears without deforming the UI, and on dismissal ONLY the Timeline resized.** ✅ **AND THE AESTHETIC HALF IS NOW ANSWERED TOO — user, 2026-09-22: *"The banner's look is perfect."*** ✅ **NOTHING REMAINS OF THIS RECORD.** ⚠️ **Both complaints it carried are resolved: (1) the layout damage, by [SP-135]'s `safeAreaBar` conversion; (2) the banner's existence, by the ruling that it is CORRECT and should stay.** ✅ **RESOLVED - awaiting the user's Verified.**  ---- ✅ **VERIFIED BY THE USER 2026-09-24.** ⚠️ **THE RECORD WAS STALE, NOT THE FIX:** ⚠️ **[SP-135]'s live pass on 2026-09-22 DID verify this — ✅ its closed record says so and [EP-040] has cited it as MET ever since** — ⛔ **but this row was never moved out of `Issue-active.md`, so the Issue layer and the Epic layer disagreed for two days.** ✅ **FOUND MECHANICALLY by the Audit Check run before [EP-040]'s close** (`docs/Audits/Audit-Check-20260924.md`, finding **F-3**), ⚠️ **exactly the failure shape that instrument exists to catch: an Issue cited as evidence for a MET AC with no archive entry.** ✅ **USER RULING 2026-09-24:** *"The records are stale. I-203 and I-205 are resolved."* |
| **I-0207** | `[Apple]`+`[ScriviCore]` ⚠️ **EVERY CLICK BACK INTO THE APP BEACHBALLS 1–3 s: `didBecomeActive` runs `reconnectWorlds()` SYNCHRONOUSLY ON THE MAIN THREAD, and it re-reads + re-parses `binding.json` ONCE PER ENDPOINT.** ✅ **MEASURED 2026-09-14 by `sample Scrivi` during a live focus stall** (`/tmp/scrivi-stall.txt`, 7,150 main-thread samples, pid 51023). ✅ **2,852 / 7,150 samples — 40% — in `reconnectWorlds`; 2,826 of those in the SINGLE synchronous `scrivi_list_pending_edges` C ABI call.** ---- ⚠️ **SECOND SAMPLE (2026-09-14 10:43, same process, focus-change + SCROLL) IS WORSE: IT FIRES TWICE PER CLICK.** ✅ **5,795 / 7,157 main-thread samples — 81% — in `reconnectWorlds`, across TWO SEPARATE ACTIVATION BRANCHES that each run the FULL sweep:** ⚠️ **(A) `sendEvent:+1296` → `-[NSWindow _handleLeftMouseDownEvent:]` → `_NXFinishActivation` → `didBecomeActive` (2,946 samples);** ⚠️ **(B) `sendEvent:+2232` → `-[NSApplication _handleActivatedEvent:]` → `didBecomeActive` (2,849 samples).** ✅ **Both bottom out identically in `listPendingEdges` → `listPending` → `resolve` → `loadBinding` → `parseWorldBinding`, with ~860 JSON-parse samples EACH.** ⚠️ **CORRECTION 2026-09-14 — MY FIRST READING OF THIS WAS WRONG.** ⚠️ **I claimed the two branches had DISTINCT `sendEvent:` offsets and were therefore two separate activations.** ✅ **They are NOT: branch A's INNER frame is `sendEvent:+2232`, IDENTICAL to branch B's** (line 39 of the sample nests `+2232` inside `_handleLeftMouseDownEvent` → `_NXFinishActivation`). ✅ **Both reach `_handleActivatedEvent:` → `didBecomeActive` → the SAME `EditorView.swift:120`.** ---- ✅ **THE ACTUAL MECHANISM: A PER-WINDOW OBSERVER RUNNING AN ALL-WINDOWS LOOP.** ⚠️ **The `.onReceive(didBecomeActive)` at `EditorView.swift:115` lives on `ManuscriptEditorView`, which is instantiated ONCE PER PROJECT WINDOW**, ⚠️ **while `reconnectWorlds()` (`AppEnvironment.swift:429`) loops `for (_, session) in openProjects.sessions` — EVERY open project.** ✅ **So N windows run N sweeps, each covering N sessions: N² SESSION-RELOADS PER ACTIVATION.** ⚠️ **The sample caught N=2.** ⚠️ **The same duplication applied to the `didMount`/`didUnmount` observers beside it.** ---- ✅ **THE SCROLL IS NOT A COST: the same sample shows `scrollDidChange` 4, `scrollWheel` 4, `NSScrollView` 4 samples.** ✅ **Scrolling is CHEAP; the stall the user felt after scrolling was the focus click, not the scroll.** ⚠️ **USER-REPORTED FIRST** (*"every click to return focus … incurs some delay (1 to 3 seconds, beachball) … due to the synchronous nature of the app"*) — ✅ **the sample CONFIRMS that reading.** ---- ✅ **THE CHAIN:** `-[NSWindow _handleLeftMouseDownEvent:]` → `_NXFinishActivation` → `didBecomeActive` → `EditorView.swift:120` `.onReceive` → `AppEnvironment.reconnectWorlds()` (`AppEnvironment.swift:432`) → `WorldWarningModel.reload` → `ScriviEngine.listPendingEdges` → `scrivi_list_pending_edges` (`scrivi_c_api.cpp:1137`) → `RelationshipStore::listPending` (`RelationshipStore.cpp:331`) → `EndpointResolver::resolve` → `WorldStore::loadBinding` (`WorldStore.cpp:203`) → `parseWorldBinding` → `nlohmann::json::parse`. ---- ⚠️ **THE MULTIPLIER IS UNCACHED RE-PARSING, NOT JUST SYNCHRONY.** ⚠️ **`listPending` (`:339`) loops EVERY EDGE × BOTH ENDPOINTS calling `resolve` each time;** ⚠️ **`loadBinding` (`:203`) does `readTextFile` + a FULL JSON parse with NO CACHE**, ✅ **so the SAME `binding.json` is re-read and re-parsed once per endpoint** (✅ **120 leaf samples in `parseWorldBinding`, 71 in `parseJson`/nlohmann parse**). ---- ⚠️ **THE TRIGGER IS CORRECT AND MUST NOT BE SIMPLY DELETED.** ✅ **[I-0123] added `didBecomeActive` deliberately** (⚠️ **re-acquire the sandbox grant BEFORE re-reporting, or a physically-returned drive stays unreadable**), ✅ **and [I-0129] added mount-watching because focus alone was insufficient.** ⚠️ **`reconnectWorlds`'s own doc-comment claims it is *"cheap enough to run on every foreground: one `listWorlds` plus one bookmark resolve per bound world"* — ⚠️ THAT ESTIMATE OMITS `worldWarning.reload`, which is where 99% of the measured cost actually is.** ---- ✅ **FIX DIRECTION (NOT YET RULED):** ✅ **(a) cache parsed `WorldBindingRecord` in `WorldStore` (the obvious win — it collapses N parses to 1);** ✅ **(b) get `listPendingEdges` OFF the main thread**; ⚠️ **(c) do NOT run the full pending-edge sweep on every foreground when nothing changed.** ⚠️ **(a) is core-side and testable by `ctest`; (b) is app-side. ⚠️ They must not be conflated.** ---- ⚠️ **RELATED: [I-0205]** — ✅ **`WorldWarningModel.reload` is what produces the `"1 world is unavailable"` banner**, ⚠️ **so the banner and this stall are the SAME subsystem and should be diagnosed together.** | ⚠️ **HIGH** | ✅ **SP-133** (T-0533 + T-0534) | ✅ **VERIFIED 2026-09-14 (user-approved after a LIVE PASS).** ✅ **All three halves landed — [T-0533] the binding cache, [T-0534] the coalescing, [T-0523] off the main thread.** ✅ **The focus-click beachball is GONE.** ✅ **HALF (a) LANDED: `WorldStore::loadBinding` now takes an optional `BindingCache`, and `listPending` owns ONE for the whole sweep** — ✅ **N reads/parses of the same `binding.json` collapse to 1.** ✅ **596/596 ctest** (⚠️ **594 + 2 new I-0207 tests**), ✅ **and the read-count test was PROVED to FAIL when the cache is disabled** — ⚠️ **a result-only test would have passed on a cache that never caches.** ---- ✅ **HALF (c) LANDED TOO (T-0534): `coalescedReconnectWorlds()` collapses a burst of activations into ONE run**, ✅ **and all three per-window observers (`didBecomeActive`, `didMount`, `didUnmount`) now call it instead of `reconnectWorlds()` directly.** ⚠️ **COALESCING, NOT JUST ONE SUBSCRIBER: `didBecomeActive` is legitimately delivered more than once around a single activation, so deduplicating the SUBSCRIBER alone would still double-run.** ⚠️ **A FLAG, NOT A DEBOUNCE — a timer would DELAY world re-acquisition, which is the very thing [I-0123] exists to prevent; this runs on the FIRST call and suppresses only redundant repeats in the same run-loop turn.** ---- ⚠️ **HALF (b) IS STILL NOT DONE: the sweep remains SYNCHRONOUS on the main thread.** ⚠️ **One sweep of a large project is still slow — ✅ the cache and the coalescing reduce it, ⚠️ they do not make it async. Do NOT close this issue yet.** ---- ✅ **HALF (b) IS ASSIGNED TO [T-0523] (user-directed 2026-09-14), NOT to a task of its own.** ⚠️ **T-0523 is AC6 — *the load runs OFF the main thread* — and it ALREADY carries the instruction to plan for [I-0198] (worker-thread widget touches) and [I-0199] (a progress surface nobody can see).** ✅ **Those two hazards are NOT load-specific: they belong to ANY worker path in this app, so a separate async task for the world sweep would re-earn both.** ⚠️ **BUT THE SURFACES DIFFER AND MUST BE DECIDED SEPARATELY: the load is a ONE-SHOT with a determinate count (a progress bar is right); the world sweep is a RECURRING refresh on activation/mount — ✅ a bar flashing on every click back into the app would be WORSE than the beachball.** ✅ **Share the threading machinery, not the surface.** ---- ✅ **HALF (b) LANDED 2026-09-14 WITH [T-0523]: `WorldWarningModel.reloadAsync` runs the `listPendingEdges` sweep on a `Task.detached` and publishes on the main actor.** ✅ **All three activation observers and the editor's `.task` now use it; ⚠️ NO synchronous `reload` caller remains.** ✅ **NO progress bar for the sweep, as ruled.** ✅ **ALL THREE HALVES LANDED AND CONFIRMED BY LIVE PASS 2026-09-14.** |
