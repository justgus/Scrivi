# Verified Issues: I-0211 – I-0220

| ID | Issue | Priority | Sprint | Verified |
| -- | ----- | -------- | ------ | -------- |
| **I-0213** | `[Apple]` ⚠️ **CREATING A SCENE (⏎) HANGS THE APP FOR 2.7 SECONDS, AND `rebuildStorage` RUNS TWICE.** ✅ **MEASURED 2026-09-14 on the user's live run:** ⚠️ **`[SCRIVI-EDIT] keyDown(⏎ code=36)=2739.7 ms`** — ✅ **against `1.7–8.6 ms` for every ordinary keystroke in the same session.** ---- ✅ **THIS IS THE CASE [T-0529] FLAGGED AS NEVER TESTED.** ⚠️ **[SP-133] recorded `rebuildStorage` at `270 ms` on TextKit 1 and noted *"every scene create/delete/merge/split pays it"*, ⚠️ but also that the user *"could not test typing or split/merge at all"* because the app was unusable.** ✅ **It is usable now, and this is the first measurement of that path.** ---- ⚠️ **DO NOT ASSUME THE CAUSE. THE INSTRUMENTED LOOP REPORTS `0.0 s`:** ✅ **`[SCRIVI-TIMING] <<< rebuildStorage took 0.0 s`, with every tick `avg=0.0ms/item`.** ⚠️ **So the 2.7 s is NOT inside the measured loop — ✅ it is the same SUBTRACTION situation that made [I-0206]'s AppKit attribution unsafe, and it must be `sample`d during a scene create before any fix is designed.** ⚠️ **THREE code-read diagnoses were wrong earlier in this investigation ([I-0204] was found by `sample`, not by reading).** ---- ✅ **ONE THING THE LOG DOES ESTABLISH: `rebuildStorage` RUNS TWICE PER CREATE.** ⚠️ **Two full `>>> entering: rebuildStorage segments=1174` … `<<< took 0.0 s` cycles fire back-to-back after the ⏎, ✅ and the same doubling appears on the later navigate-driven rebuilds.** ⚠️ **Whether the duplication CAUSES the 2.7 s or merely accompanies it is UNKNOWN** — ✅ **but a structural op rebuilding the whole 1.85 MB storage twice is worth removing on its own terms.**  ---- ⚠️ **SCOPE CORRECTED 2026-09-14 AFTER THE USER SAID WHAT THEY ACTUALLY DID** (*"I did both a scene create and a chapter create, then I did scene deletes and a chapter delete"*). ⚠️ **MY FIRST READING SAID \"scene create costs 2.7 s\" — ✅ THAT IS TOO BROAD AND THE LOG DISPROVES IT.** ✅ **SIX `rebuildStorage` EVENTS APPEAR ACROSS CREATES AND DELETES; ⚠️ ONLY ONE KEYSTROKE IS SLOW:** ✅ **segment transitions run `1174 → 1173 → 1174 → 1173 → 1173 → 1172`, ⚠️ and the deletes carry NO slow `keyDown` at all.** ⚠️ **So structural ops are NOT uniformly expensive — ✅ ONE event is, and the others are not.** ---- ⛔ **WHICH ACTION PRODUCED THE 2.7 s IS NOT DETERMINABLE FROM THIS LOG, and I stopped inferring it after two wrong readings.** ✅ **ESTABLISHED: `keyDown` RETURNS EARLY for ⌘↩ (scene) and ⇧⌘↩ (chapter) — `ManuscriptTextView.swift:2176-2186` — so those paths NEVER REACH the `defer` that logs the timing.** ⚠️ **The logged `keyDown(⏎ code=36)=2739.7 ms` is therefore a PLAIN RETURN that fell through to `super.keyDown`, NOT a ⌘↩ scene create.** ⚠️ **But the segment count rises `1173 → 1174` immediately after it, ✅ and the menu ALSO offers New Scene** — ⛔ **so whether the create came from that keystroke or from a menu action interleaved in the same moment CANNOT be told apart here.** ✅ **THE INSTRUMENTATION ITSELF IS THE GAP: the two chord paths are untimed.** ---- ✅ **FIRST STEP IS THEREFORE INSTRUMENTATION, NOT A FIX:** ✅ **time `handleCreateScene` / `handleCreateChapter` / the merge paths directly, so a structural op reports its OWN cost instead of being inferred from a keystroke that may not have caused it.** ⚠️ **THEN `sample` during the op that proves slow.** | ⚠️ **HIGH** (⚠️ **originally a 2.7 s freeze; ✅ now `~305 ms` of work**) | ✅ **[EP-040]** — ⚠️ **CARRIED FROM [EP-039] at its close 2026-09-15 (user ruling)** | 🟢 **CAUSE IDENTIFIED AND FIXED 2026-09-14 — Not Verified.** ---- ✅ **THE INSTRUMENTATION ANSWERED IT IMMEDIATELY:** ✅ **`[SCRIVI-STRUCT] createChapter=2959.5 ms` paired with `[SCRIVI-EDIT] keyDown(⏎)=2959.6 ms`** — ⚠️ **2959.5 of the 2959.6 ms is INSIDE `handleCreateChapter`; the keystroke is a PASSENGER.** ✅ **IT IS CHAPTER CREATE, NOT SCENE CREATE:** ⚠️ **the SAME run did a scene create (segments `1172 → 1173`) with NO `[SCRIVI-STRUCT]` line above the 0.5 ms threshold.** ✅ **My earlier framing — *\"scene create costs 2.7 s\"* — was wrong in BOTH the op and the attribution, and the measurement corrected it.** ---- ✅ **ROOT CAUSE: `ViewportSceneLoader.renumberChapterTitlesFrom` — the ONLY heavy call the chapter path makes that the scene path does not.** ⚠️ **It was O(chapters × scenes) WITH ONE `@Observable` WRITE PER SCENE:** ⚠️ **the inner loop scanned ALL 1,174 scenes for EVERY chapter from the split point onward (~47,000 iterations), and each match did `allScenes[j] = SceneInfo(...)`** — ✅ **`allScenes` is `@Observable` (`:47`), so that is ~1,174 notifications, each able to drive a SwiftUI update pass.** ⚠️ **SAME DEFECT CLASS AS [AC9]'s `loadAll`.** ---- ✅ **FIX, TWO STRUCTURAL CHANGES:** ✅ **(1) a `chapterID → newTitle` map collapses O(chapters × scenes) to O(scenes) — ONE pass, not one per chapter; ✅ (2) build into a LOCAL and assign ONCE — one notification for the whole renumber.** ✅ **A skip-if-unchanged guard avoids rewriting rows whose title is already correct.** ✅ **BUILD SUCCEEDED, no warnings.** ---- ⚠️ **NOT YET PROVEN TO BE THE WHOLE COST.** ⚠️ **`handleCreateChapter` spans an `await`, so other work inside it could contribute** — ✅ **`[SCRIVI-STRUCT]` is RETAINED so the next run MEASURES the result instead of assuming it.** ⚠️ **If `createChapter` is still slow, the remaining suspect is `reloadSceneDots`, which BOTH paths call — ✅ meaning it could not explain the scene/chapter asymmetry on its own.** ---- ✅ **SECOND ROUND 2026-09-14 — THE DOUBLED `rebuildStorage` IS NOW EXPLAINED AND FIXED, AND IT WAS WORSE THAN \"DOUBLED\".** ✅ **COUNTED: the chapter-split path made ~26 SEPARATE writes to `segments`/`allScenes`** (⚠️ **`insertChapterFirstScene` 3, `splitScene` 6, `splitChapter` 17**), ⚠️ **each an `@Observable` mutation able to drive its OWN `updateNSView` pass — and each pass re-derives a 1,174-element ID array plus a chapter fingerprint.** ✅ **That is the doubling seen in the log, and the earlier \"two steps\" inference was an UNDERCOUNT.** ---- ✅ **THE HEAVIEST WAS ALSO O(N²): `splitChapter`'s second loop scanned `allScenes` (1,174) and ran `segments.firstIndex(where:)` — a LINEAR scan of 1,174 — FOR EVERY MATCH.** ⚠️ **Up to ~1.4 MILLION comparisons for one chapter split.** ✅ **Replaced with ONE `sceneID → index` map (O(N)).** ---- ✅ **ALL THREE MUTATORS NOW BUILD LOCALLY AND ASSIGN ONCE.** ✅ **Property writes on the chapter-split path: ~26 → 6.** ⚠️ **`insertChapterFirstScene` was left ALONE deliberately — ✅ it already does one insert per array, which is the floor for what it does; changing it would be churn.** ✅ **BUILD SUCCEEDED, no warnings.** ---- ⚠️ **STILL NOT MEASURED. ✅ `[SCRIVI-STRUCT]` REMAINS IN PLACE so the next run reports `createChapter=` directly** — ⚠️ **three fixes now sit on this path (`renumberChapterTitlesFrom`, `splitChapter`, `splitScene`) and NONE has been confirmed against a live number.** ---- ✅ **MEASURED 2026-09-14: `createChapter` `2959.5 → 2092.5 ms`.** ✅ **A REAL 29% GAIN — ⚠️ BUT 2.1 s REMAINS, so the batching fixes were NOT the dominant cost.** ⚠️ **Recorded as a partial result rather than a success.** ---- ✅ **TWO UNTIMED BLOCKS REMAIN ON THIS PATH AND ARE NOW INSTRUMENTED:** ✅ **(1) `engine.createChapter` — a SYNCHRONOUS C ABI call ON THE MAIN ACTOR;** ✅ **(2) `timelineModel.reloadSceneDots`, which makes ONE `getSceneStoryTime` C ABI CALL PER SCENE — 1,174 synchronous calls.** ⚠️ **At ~1.8 ms each that alone would account for the missing 2.1 s** — ⛔ **BUT THAT IS ARITHMETIC-THAT-FITS, WHICH IS EXACTLY HOW [I-0206] WAS CALLED WRONG.** ✅ **`[SCRIVI-STRUCT]   engine.createChapter (C ABI)=` and `[SCRIVI-STRUCT]   timeline reloadSceneDots=` now report each directly.** ⚠️ **NO further fix until one of those lines names the cost.** ---- ⛔ **THE `createChapter` NUMBERS WERE ALL INVALID — MY INSTRUMENTATION WAS PLACED WRONG.** ⚠️ **The user reported it: *\"the chapter split includes a modal popup dialog that requires a confirmation click.\"*** ✅ **CONFIRMED IN THE CODE: `alert.runModal()` blocks until the user clicks, and my `__t0` was ABOVE it** — ⚠️ **so `2959.5` / `2092.5` / `3638.6 ms` all include HUMAN REACTION TIME.** ⚠️ **That is why they varied erratically, ⛔ and why the \"29% improvement\" I claimed from the batching fixes was NOISE, NOT SIGNAL.** ✅ **CORRECTED: the timer now starts AFTER the modal and reports `[SCRIVI-STRUCT] createChapter WORK=… (modal excluded)`.** ⚠️ **The batching fixes (`renumberChapterTitlesFrom`, `splitChapter`, `splitScene`) REMAIN — ✅ they removed real O(N²) work and ~20 redundant observable writes — ⛔ but they are NOW UNMEASURED and must not be credited until the work-only number exists.** ---- ✅ **ONE NUMBER IN THAT LOG IS REAL, because no modal touches it: `[SCRIVI-STRUCT]   timeline reloadSceneDots=971.6 ms` and `942.5 ms` — CONSISTENT ACROSS TWO OCCURRENCES.** ---- ✅ **ROOT CAUSE, READ: `TimelineViewModel.reloadSceneDots` makes ONE `getSceneStoryTime` C ABI CALL PER SCENE (1,174), synchronously on the main actor** (`TimelineStripView.swift:319`), ⚠️ **plus a `dots.first(where:)` INSIDE the map — a second O(N²).** ⚠️ **`load()` does the SAME at `:350`, which is why `TimelineViewModel.load` costs `113 ms` at open.** ---- ⚠️ **AND THIS IS [EP-039]'s OWN [AC4], UNADOPTED:** ✅ **`scrivi_list_story_times` (⚠️ **[EP-039] **AC4** — the SPARSE bulk call: ONE crossing returning a record only for scenes whose story time is EXPLICITLY set, replacing one C ABI call per scene**) EXISTS at the C ABI (`scrivi.h:72`) and was built in [SP-131] to replace exactly this per-scene read** — ⛔ **but it is NOT BOUND in `ScriviEngine`, so no Apple surface can call it.** ⚠️ **A capability shipped with no surface** (`project_capability_without_surface`). ⚠️ **AC4 is recorded VERIFIED on the CORE measurement; ✅ the core half is real, ⛔ but the Apple timeline never adopted it.** ---- ✅ **AC4 NOW ADOPTED ON APPLE 2026-09-15 (user-directed).** ✅ **`ScriviEngine.listStoryTimes(projectRootPath:)` binds `scrivi_list_story_times`**, ✅ **with `StoryTimesResult` / `SceneStoryTimeEntry` mirroring the C ABI field names exactly.** ⚠️ **THE EMPTY-ARRAY TRAP IS HANDLED ONCE, IN ONE PLACE** (`TimelineViewModel.explicitStoryTimes`): ✅ **the C ABI OMITS the `storyTimes` key entirely when nothing is set — so its ABSENCE decodes to `[]`, and `count` (always emitted) is the \"the call ran\" signal.** ⛔ **Emptiness is NEVER read as failure; ✅ only a THROW is failure, and even then the timeline proceeds with DEFAULTS rather than drawing blank** (⚠️ **a blank timeline would hide the writer's story, which is the exact harm the C ABI header warns about**). ---- ✅ **BOTH PATHS ADOPTED:** ✅ **`reloadSceneDots` (`:346`, the ~950 ms chapter-create cost) and `load` (`:318`, the `113 ms` open cost).** ⛔ **No `getSceneStoryTime` call remains in `TimelineStripView.swift`** — ✅ **grep-verified; the only match is a comment.** ✅ **ALSO FIXED: `dots.first(where:)` INSIDE the per-scene map — a SECOND O(N²) — replaced by a `sceneID → bandID` map.** ✅ **BUILD SUCCEEDED, no warnings.** ---- ⛔ **NOT MEASURED YET.** ⚠️ **The next run must show `[SCRIVI-STRUCT]   timeline reloadSceneDots=` falling from ~950 ms, and `TimelineViewModel.load` from `113 ms`.** ⚠️ **`[SCRIVI-STRUCT] createChapter WORK=` (modal excluded) is the number that finally says what a chapter create COSTS** — ✅ **every earlier figure for it was invalid.** ---- ✅ **MEASURED 2026-09-15 — AC4 ADOPTION CONFIRMED, WITH THE USER NAMING EACH ACTION** (*\"Created a Scene. Then I inserted a new Chapter at the end (no dialog). Then I created a new chapter near the end (dialog).\"*). ---- ✅ **`TimelineViewModel.load`: `113 ms` → `3.5 ms` — a 32x IMPROVEMENT.** ✅ **Total open wall clock `0.45 s` → `0.34 s`.** ✅ **`engine.createChapter (C ABI)` = `18.6–23.6 ms`** — ⚠️ **the CORE was never the cost.** ---- ✅ **THE FIRST VALID `createChapter` NUMBER: `WORK=857.3 ms` and `880.8 ms` (modal excluded), CONSISTENT ACROSS TWO CREATES** — ⚠️ **one with the confirmation dialog, one without, confirming the modal is now correctly outside the timer.** ⚠️ **The raw `keyDown(⏎)=2123.4 ms` on the dialog case is again mostly the user's click; ✅ `WORK` is the real figure.** ---- ⚠️ **BUT `reloadSceneDots` BARELY MOVED: ~950 → ~745 ms, DESPITE the per-scene C ABI calls being GONE.** ✅ **THE DECISIVE COMPARISON: `load()` builds the SAME dots and now costs `3.5 ms`** — ⚠️ **so the cost is NOT in dot construction; it is in something `reloadSceneDots`'s block does that `load()` does NOT.** ---- ✅ **ROOT CAUSE: `updateDotTitles`, which my timer SPANNED but did not NAME.** ⚠️ **The label was misleading and hid this for a full round.** ⚠️ **It was DOUBLY O(N²): (1) `allScenes.first(where:)` — a 1,177-element LINEAR SCAN PER DOT, ~1.4 M comparisons; (2) `dots[i].title = …` — an `@Observable` write PER DOT, up to 1,177 notifications.** ✅ **FIXED: one `sceneID → SceneInfo` map, ONE assignment to `dots`, and the write is SKIPPED ENTIRELY when no title changed.** ✅ **The two functions are now TIMED SEPARATELY (`reloadSceneDots=` / `updateDotTitles=`) so a mislabelled timer cannot hide a cost again.** ✅ **BUILD SUCCEEDED, no warnings.** ✅ **MEASURED 2026-09-15 — BOTH PREDICTIONS CONFIRMED.** ✅ **`updateDotTitles`: ~745 ms → `1.1–1.2 ms` (a ~600x collapse).** ✅ **`createChapter WORK`: `857/880 ms` → `305.4 / 319.6 / 346.2 ms` — a ~2.7x improvement, CONSISTENT ACROSS THREE CHAPTER CREATES in one session.** ✅ **`reloadSceneDots`: ~950 ms → `195.6–203.7 ms`.** ✅ **`engine.createChapter` (C ABI) `18.6–24.2 ms` — the core remains a rounding error.** ✅ **`TimelineViewModel.load` holds at `3.6 ms`; open wall clock `0.34 s`.** ---- ⚠️ **WHAT REMAINS IN `createChapter WORK` (~305 ms): `reloadSceneDots` is now ~200 ms of it — ✅ roughly two-thirds.** ⚠️ **The per-scene C ABI calls are GONE, so this is the remaining Swift-side dot construction over ~1,179 scenes plus `recomputeAllOffsets`.** ⛔ **NOT YET ATTRIBUTED FURTHER — do not guess; instrument inside `reloadSceneDots` if it is worth pursuing.** ---- ⚠️ **NOTE ON THE SAME RUN, NOT A DEFECT: the resign shows `dirty=2` and a full re-index.** ✅ **That is AC10 behaving CORRECTLY — two scenes genuinely had unsaved bodies, so re-indexing is required.** ⚠️ **Recorded because it superficially resembles the AC10 failure and should not be misread as one.** ---- ⚠️ **ALSO NOT A DEFECT: `boundaryStart` and `mapSaid` diverge (e.g. `1851633` vs `1850954`) AFTER the user enabled \"Show Chapter Titles\".** ✅ **That is [I-0131]'s documented divergence — the map is heading-unaware while boundaries are authoritative, which is exactly why `restoreWritingSurface` prefers boundaries.** ---- ✅ **INSTRUMENTATION SHIPPED 2026-09-14 (build clean):** ✅ **`[SCRIVI-STRUCT] createScene / createChapter / mergeScene / mergeChapter` now time each structural op DIRECTLY**, ⚠️ **closing the gap that made this un-attributable.** ✅ **THE NEXT RUN DECIDES IT: if a `[SCRIVI-STRUCT]` line reads ~2.7 s, that op is the cause; ⚠️ if the hang recurs with NO `[SCRIVI-STRUCT]` line, it was a PLAIN RETURN and the cost is in AppKit's own newline handling** — ✅ **OPPOSITE fixes, which is exactly why it was not guessed.** ---- ⚠️ **ON THE DOUBLED `rebuildStorage`:** ✅ **the `updateNSView` guard DOES cache its inputs BEFORE rebuilding (`ManuscriptTextView.swift:169-172`), so a second IDENTICAL pass would be SKIPPED.** ⚠️ **The doubling therefore implies `updateNSView` ran twice with GENUINELY DIFFERENT segment IDs — ✅ consistent with a structural op mutating `segments` in TWO steps.** ⛔ **NOT PROVEN; it needs the same run.**  ---- ✅ **VERIFIED BY THE USER 2026-09-24 ON `dumas-prose-timelines` (1,179 scenes, 1.85 MB), WITH A CONSOLE PASTE.** ⚠️ **The user's words:** *"The performance of chapter create and chapter merge are acceptable in medium to large projects… At the end of the document I typed a paragraph, merged some chapters and scenes, created some chapters and all through did not notice any degraded performance. Chapter create and merge were a little slow, but they were acceptable."* ---- ✅ **TWO CHAPTER CREATES IN THAT RUN, AND THE SECOND IS THE STEADY-STATE FIGURE:** ✅ **`createChapter WORK=879.5 ms` (first, cold) and `WORK=396.7 ms` (second)**, ✅ **with `reloadSceneDots` `629.2 → 235.9 ms` and `updateDotTitles` `1.1 / 1.2 ms` HOLDING at the ~600x collapse measured on 2026-09-15.** ✅ **`engine.createChapter` (C ABI) `113.6 / 70.6 ms` — ⚠️ higher than the `18.6–24.2 ms` seen before, ✅ but still a small share and NOT the cost.** ---- ✅ **THE NUMBERS ARE HIGHER THAN 2026-09-15's `305.4 / 319.6 / 346.2 ms` BY DESIGN — THIS WAS A DELIBERATE WORST-CASE TEST, AND THAT IS WHAT MAKES IT A VERIFICATION.** ⚠️ **The user stated the intent:** *"I opened the project and world from the Scrivi Worlds mount point deliberately so that performance would be as poor as possible (writing to a USB stick)… A good test for acceptability."* ✅ **So `WORK=396.7 ms` steady-state is a FLOOR measured on the SLOWEST medium available, not a typical-case sample** — ⚠️ **internal-SSD performance can only be BETTER, and the earlier `~305 ms` figures are the fast-medium comparison, not the standard this run failed to meet.** ⛔ **MY FIRST READING TREATED THE USB MOUNT AS A CAVEAT THAT WEAKENED THE RESULT; ✅ IT IS THE OPPOSITE.** ---- ⚠️ **THE USER ALSO NAMED THE NEXT PLATFORM'S EXPECTATION:** *"On Linux it will be even worse because it has to also come through this computer's operating system."* ✅ **RECORDED SO THE LINUX PORT INHERITS THE EXPECTATION RATHER THAN RE-DISCOVERING IT** — ⚠️ **a Linux figure WORSE than `396.7 ms` on the same fixture is ANTICIPATED, not a regression against this record** (⚠️ **the rig reaches that volume through the host OS as well as the guest**). ✅ **THE VERIFICATION IS THE USER'S JUDGEMENT THAT IT IS ACCEPTABLE IN REAL WRITING — ⛔ NOT a claim that a specific millisecond target was hit.** ---- ✅ **TYPING WAS EXERCISED AT THE DOCUMENT END, WHICH IS THE TEST [I-0206] WAS ONCE WRONGLY CLOSED WITHOUT:** ✅ **`[SCRIVI-KEY] total=2.4–3.2 ms` across ~300 logged keystrokes at `len≈1,852,000`, with `didChangeText` `2.4–4.7 ms`.** ⚠️ **`keyDown` remains `65–99 ms`, which is [I-0206]'s AppKit cost and is NOT this Issue's** — ✅ **the user found it acceptable, ⛔ but that is a separate record and this verification does not close it.** ---- ✅ **THE DOUBLED `rebuildStorage` THIS ISSUE ALSO NAMED IS GONE AS A COST:** ⚠️ **rebuilds still appear in pairs on some paths, ✅ but each reports `took 0.0 s` at `segments=1180`, so the duplication no longer buys a stall.** ---- ⚠️ **WHAT IS NOT CLOSED BY THIS:** ✅ **`reloadSceneDots` is still roughly two-thirds of `createChapter WORK` and was never instrumented INTERNALLY** — ⚠️ **AC11 asked for that and it was NOT done** (⚠️ **[EP-040] is `[Apple]` *The Editor Shell* — build Scrivi as the kind of app it actually is; ✅ its **AC11** is the CARRIED performance clause: the manuscript surface's remaining O(DOCUMENT) costs are addressed OR accepted as limitations WITH a measurement**). ✅ **It is accepted as a LIMITATION WITH A MEASUREMENT, which is what AC11's second branch permits.** ---- ✅ **THE REMAINDER IS NOW MEASURED AT THE CORE, 2026-09-24 — AC11's INSTRUMENTATION REQUEST IS SATISFIED, AND THE ANSWER IS NOT WHERE THE SWIFT SIDE WAS LOOKING.** ⚠️ **NEW TEST: `ScriviCore/tests/integration/StructuralOpIndexRebuildTests.cpp`** — ✅ **it counts FILESYSTEM CALLS via `CountingFileSystem`, NOT wall clock, ⚠️ because a warm local page cache hides exactly the cost that a USB mount exposes** (⚠️ **the same reasoning `OpenProjectReadAmplificationTests` records for [I-0231]** — ✅ **[I-0231] `[ScriviCore]`: project open re-read every sidecar ~96x (55,574 reads / 8,044 opens on a 150-file project), LOCATED BY MEASUREMENT not by reading, and invisible on local disk at `0.21 s` while costing `154 s` on a `cache=none` mount**). ---- ✅ **MEASURED ON A 400-SCENE / 20-CHAPTER PROJECT BUILT THROUGH THE REAL CORE:** ✅ **`createChapter` ITSELF = 50 filesystem calls (45 reads).** ⛔ **THE `ProjectIndex` REBUILD IT FORCES = 866 calls (866 reads) — ~17x THE OP'S OWN COST.** ✅ **A WARM `explicitStoryTimes()` costs ZERO filesystem calls**, ⚠️ **so the entire expense is the REBUILD, not the query.** ---- ✅ **MECHANISM, CONFIRMED BY MEASUREMENT RATHER THAN BY READING:** ⚠️ **every structural op holds a `ProjectIndexInvalidation` guard (`scrivi_c_api.cpp:366`) that drops the cached index on BOTH sides of the write; ✅ the next `scrivi_list_story_times` therefore MISSES the cache and `ProjectIndex::build` re-resolves the WHOLE manuscript** (⚠️ **~2.15 reads per scene — the scene sidecar plus chapter sidecars; ✅ it does NOT read scene BODIES, so the pass is already near-minimal PER PASS**). ⛔ **THE WASTE IS THAT THE PASS HAPPENS AT ALL, and it SCALES WITH MANUSCRIPT SIZE while the op itself does not** — ✅ **which is precisely why it surfaces on the USB rig and is invisible on internal storage.** ---- ⛔ **THE INVALIDATION IS LOAD-BEARING AND MUST NOT SIMPLY BE REMOVED.** ✅ **It fixed a REAL correctness defect** (⚠️ **[EP-039] `[Cross]` *Project Load Performance* — its goal was that a project of ORDINARY SIZE opens without freezing the app, ✅ and that the core stops answering per-item questions with full-tree traversals; ⚠️ it replaced a `~300 s` frozen open, of which `234–251 s` was the timeline asking `getSceneStoryTime` ONCE PER SCENE. ✅ **Its AC5b** is the index-correctness clause: a failed index update DROPS THE WHOLE INDEX and the next query rebuilds and returns the CORRECT answer)** — ⚠️ **the concrete defect AC5b caught: with before-only invalidation, `scrivi_set_scene_story_time` wrote correctly to disk and `scrivi_list_story_times` then reported `count:0`, because the mutation's own locator lookup rebuilt the index FROM PRE-WRITE STATE.** ⚠️ **ANY FIX MUST KEEP THAT PROPERTY — ✅ so the direction is to make the REBUILD INCREMENTAL (a structural op knows exactly which chapter changed), NOT to invalidate less.** ⛔ **NO FIX IS WRITTEN YET; ✅ this records the measured cause and the constraint any fix must respect.** |
| **I-0214** | `[ScriviCore]` ⚠️ **An imported timeline that FAILS TO PARSE is discarded in SILENCE.** ✅ **FOUND BY THE USER 2026-09-15**, ⚠️ **who rejected the first diagnosis on evidence: the timelines HAD displayed on the previous run.** ✅ **PROVEN: same 4 files — old Swift decoder → 4 rows/157 events; core parser → 0 rows.** ⚠️ **T-0502's regression, and the core reported nothing.** | **High** | SP-129 | 2026-09-15 |
| **I-0215** | `[Apple]` ⛔ **Apple SILENTLY DISCARDED any `inspector-layout.json` key it did not know, on every save** — ⚠️ **`InspectorLayoutDocument` was a fixed 6-property `Codable` struct, so a round trip through Apple DROPPED unknown keys, while LINUX deliberately preserved them.** ⚠️ **The user opens the same projects on both.** ✅ **FIXED by retaining the raw document alongside the typed view and merging on save** (T-0536). ⚠️ **TOP-LEVEL keys only — nested-in-card keys remain lost, deliberately deferred to [SP-141].** ⛔ **Proven by a test demonstrated to FAIL without the fix.** | **High** | SP-140 | 2026-09-18 |
