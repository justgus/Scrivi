# Verified Issues — I-0191 … I-0200

⚠️ **New decade file.** The previous decade closed at **I-0190**
([`Issue-verified-0181-0190.md`](Issue-verified-0181-0190.md)).

⚠️ **The opening entry came from T-0478's LIVE PASS on the real rig** (SP-124) and ⚠️ **was found by
the USER** — ⚠️ **it was NOT findable from the suite**: it needs a real blocking `cifs` mount to show
its 102 s, and ✅ **Docker has no dead network share**, so the timeout branch was believed-in but
unobserved until a writer sat in front of it.

⚠️ **It also took TWO passes to close.** ⚠️ **The first fix (build 34) covered the scene-click path and
was proven on the rig — and left the SECOND call site the Issue had NAMED FROM THE START untouched**,
so `Project > Manage Worlds` froze identically. ⚠️ **An Issue that names two call sites is not closed by
fixing one**, however well the first is proven.

| ID | Title | Severity | Sprint | Status |
| -- | ----- | -------- | ------ | ------ |
| **I-0193** | `[Linux]` ⚠️ **SELECTING A SCENE FREEZES THE APP FOR OVER 100 SECONDS when a world's volume is unreachable — the writer's only escape is Force Quit.** ⚠️ **Found by the USER on the REAL RIG during T-0478's live pass, 2026-09-08**, with `the-stairs-of-tintagael.scrivi` open and Eskandar on a killed `cifs` share. ⚠️ **The app froze on a SCENE CLICK, showed a "Scrivi is not responding" dialog after ~6 s, stayed frozen ~4–5 MINUTES, and was Force Quit** — ⚠️ **which for a writing application is DATA LOSS, not latency.** **Measured at the ABI the same session:** ⚠️ **`scrivi_world_probe` blocked `1m42.2s` and `1m42.4s` on two consecutive runs** against ✅ **a `0.087 s` healthy baseline** — ⚠️ **~1,175× slower, and reproducible.** ⚠️ **`user`+`sys` ≈ 0.005 s: the process is doing NOTHING but waiting on blocking I/O.** **Call chain, read in the code:** `EditorShell::selectNavigatorScene` (`EditorShell.cpp:1029`) → `inspector_->setScene()` (`SceneInspector.cpp:318`) → `reload()` (`:330`) → ⚠️ **`bridge_->listWorlds(projectRootPath_)` (`SceneInspector.cpp:395`), synchronous, on the UI thread.** ⚠️ **SP-124's FINDING 1 UNDERSTATED THIS IN TWO WAYS.** ⚠️ **(1) It measured ~10 s; the real figure on this mount is TEN TIMES that** — the mount carries `cache=none,actimeo=1,closetimeo=1,echo_interval=60`, ⚠️ **so the cost is MOUNT-TUNING DEPENDENT and "~10 s" must not be treated as the ceiling.** ⚠️ **(2) It was read as a Worlds-dialog concern; the blocking call is on `setScene`** — ✅ **so the trigger is ORDINARY NAVIGATION, the most common action in the app**, not an occasional dialog. ⚠️ **`WorldsDialog::reload()` (`WorldsDialog.cpp:176-180`) has the SAME defect and was simply not the path the user hit.** ✅ **The sprint already ruled the constraint:** *"the app MUST NOT call the core synchronously on the UI thread for world status."* ⚠️ **T-0478 shipped the core status change WITHOUT it.** ⚠️ **A fix must cover BOTH call sites and needs a TIMEOUT as well as a thread** — ✅ **the healthy path is ~1,175× faster, so a timeout is cheap and highly discriminating.** ⚠️ **Related: [I-0182]** (same `listWorlds`-at-`:395` path, different symptom) and ⚠️ **[I-0192]** (the same volume-away area, opposite sign). ---- ⚠️ **LIVE-PASS RESULT, REAL RIG, 2026-09-09 (build 34 = `e978360` + stamp):** ✅ **THE SCENE-CLICK FREEZE IS FIXED AND PROVEN.** ⚠️ **Verified the way the suite structurally cannot** (`feedback_live_pass_finds_what_suites_cannot`): `the-stairs-of-tintagael.scrivi` open, File Sharing switched OFF mid-session, then a scene changed. ✅ **The scene changed IMMEDIATELY, the app stayed fully responsive, and the writer could type.** ✅ **After 5 s the writer-facing string appeared:** *"This scene's objects are taking longer than expected to read — a world may be on a disconnected or unreachable volume. Nothing has been lost."* — ⚠️ **`SceneInspector.cpp:407-409`, a branch that had NEVER EXECUTED anywhere before this pass** (Docker has no dead network share, so the timeout path was believed-in but unobserved). ✅ **THIS UNBLOCKS T-0478's REMAINING DoD ITEM** — the offline string has now been READ by a writer. ⚠️ **BUT THE FIX IS PARTIAL — DO NOT MARK RESOLVED.** ⚠️ **`Project > Manage Worlds` STILL FREEZES IDENTICALLY: five "Scrivi is not responding" dialogs, then Force Quit.** ⚠️ **`WorldsDialog.cpp` WAS NEVER TOUCHED BY THE FIX** — `git show e978360 --name-only` lists only `SceneInspector.cpp/.hpp` and `WorldStore.cpp`. ⚠️ **`WorldsDialog::reload()` (`WorldsDialog.cpp:180`) is still a bare synchronous `bridge_->listWorlds(projectRootPath_)` on the UI thread, called from the constructor at `:173`** — ✅ **exactly the second call site THIS ISSUE ALREADY NAMED as requiring the same fix.** ---- ✅ **ASYNCCALL THREAD COST — MEASURED, BOUNDED, NOT A LEAK.** ⚠️ **The timeout frees the UI but does NOT cancel the syscall** (`AsyncCall.hpp` says so). **Measured on the rig with the share dead:** ⚠️ **10 scene changes parked 10 threads** (`/proc/<pid>/task`: 18 → 28), ✅ **which drained UNATTENDED back to 16 within a few minutes with the app IDLE and untouched** — ✅ **one thread per click, released by the kernel on its own.** ✅ **The `cifs` mount is `soft`** (confirmed in `/proc/mounts`), ⚠️ **which is the load-bearing premise of AsyncCall's "parked, not leaked" claim — a `hard` mount, the cifs DEFAULT, would invalidate it.** ⚠️ **Residual risk, NOT observed: the global `QThreadPool` is core-count bounded (12 on this rig), so SUSTAINED rapid clicking on a dead share could exhaust it transiently, during which further calls QUEUE without starting.** ✅ **Ordinary use does not reach this.** ---- ⚠️ **NOTE ON A WRONGLY-FILED SYMPTOM:** ⚠️ **it was observed mid-pass that the app showed NO warning triangle after the share died, and this was nearly filed as "the app has gone silent about offline worlds".** ✅ **THAT WAS WRONG** — ⚠️ **the status DOES surface, via the TIMEOUT branch rather than via `applyReload`, which reads world NAMES only and never world STATUS.** ✅ **The writer is informed.** ⚠️ **What remains genuinely open is whether a PERSISTENT indicator is wanted in addition to the transient per-load message — a DESIGN question, not a defect.** | **High** | **SP-124** — ⚠️ **found by T-0478's live pass** | ✅ **VERIFIED 2026-09-10 (user-approved)** — ⚠️ **BOTH call sites are now async, which is the closing condition this Issue set for itself.** ✅ **Second call site fixed in `571fac1`:** `WorldsDialog::reload()` moved onto `AsyncCall::run` with a loading state, and ⚠️ **`EditorShell::writerFacingError()`'s synchronous `listWorlds()` replaced by a cached `worldNames_` hash** — ⚠️ **that call ran ONLY when a world was already known unusable, so it hit the dead volume EVERY time it executed, blocking ~102 s while composing the sentence explaining that very failure.** ⚠️ **The error handler froze the app to explain the error.** ✅ **A cache miss is HARMLESS by construction** — every branch already had a `name.isEmpty()` form, so an unknown world degrades to anonymous phrasing instead of blocking. ✅ **VERIFIED BY THE USER ON THE REAL RIG:** ⚠️ **`Project > Manage Worlds` now OPENS with the share down** — the freeze-to-Force-Quit is gone — ✅ **and the async calls time out through the `soft` mount and surface the correct message in the Scene Inspector.** ⚠️ **Verified the only way this defect CAN be** (`feedback_live_pass_finds_what_suites_cannot`): ✅ **a real blocking mount, from the writer's seat.** |

---
| **I-0195** | `[Linux]` ⚠️ **Project open BLOCKED the UI for the whole read, with no progress — and the cost scaled with world size and network speed.** ✅ **USER RULING (2026-09-08): waiting is ACCEPTABLE; waiting with a FROZEN, SILENT UI is NOT** — ✅ **and the user supplied the design: the file COUNT is known early, so `files read / files to read` is a genuine determinate percentage, not a spinner pretending to be one.** ✅ **[SP-128] moved `EditorShell::load` off the UI thread (`AsyncCall`, reusing [I-0193]'s machinery) and added a DETERMINATE `n of m scenes` bar.** ⛔ **But it stayed unverified for 10 days, and rightly so**: ⚠️ **its own status line scoped itself honestly — *"Docker has NO slow mount, so responsiveness under `cache=none` — the actual defect — is unproven"*** — ⚠️ **and when that rig pass ran (2026-09-18) THE FIX FAILED IT**, ✅ **because `Landing.qml` opened the project SYNCHRONOUSLY first ([I-0232]), so the app was frozen for 155 s BEFORE this Issue's bar could appear.** ✅ **VERIFIED 2026-09-20 under [SP-144], on the real rig at `cache=none`**, ⚠️ **once [I-0232] converted that call site**: ✅ **both bars now appear and update — INDETERMINATE on the launch screen (the landing has no scene count yet), then DETERMINATE in the editor** — ✅ **and the open fell 24.01 s → 13.46 s.** ⚠️ **THE LESSON THIS ISSUE IS WORTH REMEMBERING FOR: a fix whose own scope note says it is unproven MUST NOT be marked Verified on a green suite.** ✅ **Its machinery was correct all along; ⛔ its call site was not the only one.** | **Medium** | **[SP-128]** → verified under **[SP-144]** | 2026-09-20 |
| **I-0196** | `[ScriviCore]` ⚠️ **`scrivi_open_scene` RESOLVES THE WHOLE MANUSCRIPT to find ONE scene, so opening a project is O(N²) and a 1,152-scene project FREEZES THE APP FOR ~4.4 MINUTES.** ⚠️ **Found by the USER on macOS, 2026-09-10**, double-clicking the Dumas fixture in Finder: *"It froze hard and I had to Kill it."* ✅ **THE STACK NAMES IT EXACTLY** — `parseSceneMeta` ← `ManuscriptOrderResolver::resolve` ← `ScriviCore::openScene` ← `scrivi_open_scene` ← `ViewportSceneLoader.loadScene` ← `loadAll` — ⚠️ **all on the MAIN THREAD.** **Root cause, read in the code:** `ScriviCore::openScene` (`ScriviCore.cpp:82-83`) calls `ManuscriptOrderResolver::resolve` ⚠️ **to locate a single scene**, and `resolve` (`ManuscriptOrderResolver.cpp:51-57`) ⚠️ **reads AND JSON-parses the sidecar of EVERY scene in the manuscript.** ⚠️ **`ViewportSceneLoader.loadAll` then calls `openScene` ONCE PER SCENE**, ✅ **so a full load is N resolves × N parses = N² sidecar parses** — ⚠️ **1,152 scenes ⇒ ~1.33 MILLION file-read+parse operations.** ✅ **MEASURED ON macOS (local SSD, warm cache, -O2):** ⚠️ **`scrivi_open_project` = `5,490 ms`; ONE `scrivi_open_scene` = `228 ms`; ⇒ `1,154 × 228 ms` = **`263 seconds` = 4.4 MINUTES** of blocking main-thread work.** ⚠️ **THIS IS NOT A HANG — it is real work, which is why no deadlock appears in the stack and why Force Quit was the only escape.** ⚠️ **SEVERITY IS HIGH AND THE REASON IS SCALE-INDEPENDENT: the cost is QUADRATIC, so it is invisible at 16 scenes (`the-sentinel-of-centauri`) and fatal at 1,152.** ✅ **A real manuscript reaches 1,000+ scenes — this is an ORDINARY project size, not a stress test.** ✅ **FIX DIRECTION — the core, not the app:** ⚠️ **`openScene` must NOT resolve the whole manuscript to find one scene.** ✅ **Either (a) resolve once and pass the result in, or (b) locate the scene by its OWN sidecar** (EP-027 made scenes filesystem-authoritative, so a scene's metadata path is derivable without walking every chapter). ⚠️ **[I-0195]'s async+progress work ([T-0499]/[T-0500], Linux) MITIGATES this but DOES NOT FIX IT** — ✅ **it moves the wait off the UI thread and makes it visible** — ⚠️ **a 4.4-minute wait is still unacceptable, and Apple has NO async path at all.** ⚠️ **Distinct from [I-0195]: that is "the UI blocks during a legitimate read"; this is "the read is ALGORITHMICALLY WRONG."** ✅ **Fixing this shrinks [I-0195]'s problem by orders of magnitude.** | ⚠️ **HIGH** | ⚠️ **Unassigned** — ⚠️ **found while verifying the SP-128 fixture** | 🟢 **ROOT CAUSE FOUND AND MEASURED 2026-09-10 — instrumented, not inferred.** ✅ **`TimelineViewModel.load` is `251.02 s` of a `321.10 s` project open — 78% of the wall clock** (`TimelineStripView.swift:246`). ⚠️ **It calls `engine.getSceneStoryTime` ONCE PER SCENE**, ✅ **and that call measures a FLAT `228 ms` regardless of position** (#0 `228.09`, #577 `224.13`, #1153 `227.41`) — ⚠️ **because `getSceneStoryTime` runs `findSceneMetaPath`, which WALKS THE MANUSCRIPT to locate ONE sidecar.** ✅ **`1,153 x 228 ms = 263 s`, CONFIRMED INDEPENDENTLY at the core (`260.6 s` full sweep).** ⚠️ **THE IRONY WORTH RECORDING: `263 s` was my ORIGINAL WRONG PROJECTION for `openScene`. The number was REAL; I attached it to the WRONG FUNCTION.** ⚠️ **PREDICTIONS I MADE THAT THE DATA REFUTED — recorded because guessing is what cost the time:** ⚠️ **`rebuildStorage` (`<<< took 0.0 s`), Spotlight donate (`0.28 s`), `validateScenes` (`0.01 s`), workspace I/O (`0.3 s`) — ALL CLEARED.** ✅ **THREE DEFECTS, ONE SHAPE — a PER-SCENE call that walks the WHOLE MANUSCRIPT for ONE file:** ⚠️ **(1) `getSceneStoryTime` `251 s`; (2) `openScene` `70 s` (halved today, still O(N²)); (3) `rebuildStorage`'s nested `allScenes.first(where:)` — real, not yet biting.** ✅ **FIX DIRECTION: resolve ONCE and pass it in, or return story-time WITH the scene list — `open_project` already does exactly that for scenes.** ⚠️ **NOT YET FIXED. The instrumentation (`ScriviDiag.swift`) stays until it is.** 🟡 **PARTIALLY FIXED - Not Verified.** ⚠️ **MY FIRST ANALYSIS WAS WRONG AND IS CORRECTED HERE.** ⚠️ **I claimed the freeze was `263 s` of honest work and that `findScene` made it `9.2 s`.** ⚠️ **THE USER REFUTED THIS: *"I left it running for over an hour and it was still frozen."* ✅ **They are right and the projection was invalid** — ⚠️ **it multiplied 1,154 by the cost of opening the FIRST scene, which is the CHEAPEST one.** ✅ **MEASURED PROPERLY (a real full load, not an extrapolation): `74 s` at the CORE**, ⚠️ **and the per-200-scene cost RISES through the run — `3.7 s` for the first 200, `~17 s` for the last 200** — ✅ **so it is STILL QUADRATIC after the fix.** ⚠️ **`findScene` reduced the CONSTANT, not the COMPLEXITY:** ✅ **measured `resolve()` = `230 ms`; `findScene()` on the LAST scene = `114 ms`** — ⚠️ **only ~2× better, because `findScene` STILL walks every chapter and reads every scene sidecar in each until it matches; it only skips the second PARSE.** ⚠️ **The fixture is 1,153 scenes in just 49 chapters**, so a chapter scan is long. ⚠️ **AND `74 s` STILL DOES NOT EXPLAIN AN HOUR** — ⚠️ **there is app-side cost on top of the core that is NOT yet measured** (`ViewportSceneLoader.loadAll` builds one giant text document; the console also shows `_NSDetectedLayoutRecursion` and repeated Spotlight `donate`/`extract` cycles). ⚠️ **THE REMAINING WORK IS TO MEASURE THE APP LAYER, NOT TO ASSUME IT.** ✅ **What IS proven: the core got ~2× faster and a regression test pins that `openScene` no longer re-parses other scenes' sidecars** (verified failing at `2 <= 1` against the unfixed core); ✅ **`ctest` 586/586.** | ✅ **VERIFIED 2026-09-12 (user-approved).** ✅ **MEASURED ON THE REAL 1,153-SCENE FIXTURE, before and after, same harness:** ⚠️ **`get_scene_story_time` x1154 `251,800 ms` → ✅ `328 ms` (767x)**; ⚠️ **`open_scene` x1154 `70,552 ms` → ✅ `356 ms` (198x)**; ✅ **the two hot phases `322.4 s` → `0.68 s`.** ✅ **THE QUADRATIC IS GONE, and that is the actual claim:** ⚠️ **the last-100 average was `110.29 ms` against a first-100 of `12.81 ms`** — ✅ **after, both are `~0.30 ms`, so per-scene cost no longer depends on how many scenes precede it.** ✅ **CONFIRMED IN THE USER'S OWN CONSOLE 2026-09-12: `WALL CLOCK 1.06 s`** (was `298–321 s`), `openScene` avg `0.51 ms`, `TimelineViewModel.load` `0.11 s` (was `234–251 s`). ✅ **`ctest` macOS 594/594, Linux 598/598 (non-root); 23/23 Linux smokes; macOS+iOS+visionOS all build.** ⚠️ **THE FREEZE THIS ISSUE NAMES IS FIXED; THE APP IS STILL NOT USABLE** — ✅ **that is [I-0200]** (navigator rebuild + a 1.8 MB single text view + per-resign Spotlight re-index), ⚠️ **which is a DIFFERENT set of defects on the SAME manuscript and must not be folded back into this one.** |
| **I-0198** | `[Linux]` ⚠️ **A REGRESSION I INTRODUCED IN [T-0499]: two signals emitted FROM THE WORKER THREAD were delivered by DIRECT connection, so their handlers touched WIDGETS off the UI thread.** ⚠️ **Reported by the USER on the rig, 2026-09-11: *"when I click on a project in the recent projects list it just moves to the top. It never opens, the Manuscript View window is never presented."*** ⚠️ **AND the progress bar [T-0500] shipped never appeared at all.** **Root cause:** ⚠️ **T-0499 moved `openProject` + the per-scene `openScene` loop onto a worker via `AsyncCall`** — ⚠️ **but TWO signal connections were left at Qt's default `AutoConnection`, and BOTH have a UI-thread sender and a UI-thread receiver**, ✅ **so Qt resolves them to a DIRECT CALL and the slot runs ON WHICHEVER THREAD EMITTED** — ⚠️ **here, the worker.** ⚠️ **(1) `loadProgress` → `onLoadProgress` (`EditorShell.cpp:282`) touches `progressBar_`/`progressLabel_`** — ✅ **this is why the bar never appeared.** ⚠️ **(2) `ScriviBridge::errorOccurred` → the inline lambda (`EditorShell.cpp:334`) calls `errorLabel_->setText()` and `->show()`** — ⚠️ **and `openProject`/`openScene` EMIT that signal on any failure (`ScriviBridge.cpp:126`)** — ✅ **so ANY failure during a load manipulated widgets from the worker, which is UNDEFINED BEHAVIOUR in Qt and wedges the open path.** ⚠️ **THAT IS THE USER'S SYMPTOM: the click fires, `load()` starts, something fails, and the editor never appears.** ⚠️ **`AsyncCall.hpp`'s OWN HEADER STATES THE RULE THIS BROKE** — *"The callable runs on a worker thread… It must NEVER touch widgets, models, or any Qt object owned by the UI thread"* — ⚠️ **I wrote that warning in the same sprint and then routed two signals straight through it.** ✅ **FIX: `Qt::QueuedConnection` on both**, which forces delivery on the RECEIVER's thread. ⚠️ **THE SMOKES CANNOT CATCH THIS AND DID NOT** — ✅ **23/23 passed BEFORE the fix as well**: they run `offscreen`, exercise read paths, ⚠️ **and a cross-thread widget touch is undefined behaviour that need not crash under a harness.** ⚠️ **Same structural blindness that let [I-0193]'s freeze reach a writer** (`feedback_live_pass_finds_what_suites_cannot`). ⚠️ **BLOCKS [T-0501] and therefore the EP-038 close.** | ⚠️ **HIGH** | ✅ **SP-128** — ⚠️ **a regression in this sprint's own work** | ✅ **VERIFIED 2026-09-11 (user-observed on the rig, build 41)** — ⚠️ **the only way this COULD be verified.** ✅ **A recents click on `/mnt/scrivi-net/projects/the-stairs-of-tintagael.scrivi` (60 scenes, ON THE NETWORK SHARE) opened the Manuscript View**, ⚠️ **and the trace showed the previously-missing line: `onDone ok=1` → `loadFinished ok=1`.** ✅ **Trace scaffolding removed; 23/23 smokes.** ⚠️ **SECOND DIAGNOSIS; THE FIRST ONE WAS INCOMPLETE AND DID NOT WORK.** ⚠️ **Build 39 shipped the queued-connection fix and the symptom was UNCHANGED**: no editor, no progress bar. ⚠️ **I had read the code and guessed TWICE.** ✅ **Instrumenting the path found it in ONE run — and Qt had been saying it aloud the whole time:** `QObject::connect(EditorShell, ScriviWindow): unique connections require a pointer to member function of a QObject subclass`. ✅ **ROOT CAUSE: `Qt::UniqueConnection` IS NOT SUPPORTED WITH A LAMBDA.** ⚠️ **Qt REJECTED the entire connection**, ✅ **so `loadFinished` had NO LISTENER.** ⚠️ **The trace proves the load itself was FINE all along** — `WORKER openProject returned, mode=ready` → `WORKER done, scenes=20` → `onDone ok=1` — ⚠️ **and then NOTHING, because the view-stack switch was never wired.** ⚠️ **My comment claimed UniqueConnection prevented stacked handlers; ✅ it is not needed at all — the connect is INSIDE `if (editor_ == nullptr)`, which runs once per window.** ✅ **FIX: drop the flag.** ⚠️ **THE QUEUED-CONNECTION CHANGES ARE KEPT** — ✅ **they fix a REAL second defect** (worker-thread signals touching widgets, which `AsyncCall.hpp` forbids) — ⚠️ **but they were NOT this symptom's cause.** ⚠️ **LESSON, and it cost two rounds: `qInfo` breadcrumbs found in one run what reading found in none** (`feedback_prove_code_is_reached`). ⚠️ **Qt's own runtime warning named the bug before I did.** ✅ **Builds clean.** ⚠️ **NOT VERIFIED: needs a recents click that OPENS the editor on the rig.** |
| **I-0199** | `[Linux]` ⚠️ **THE PROGRESS BAR [T-0500] SHIPPED CAN NEVER BE SEEN — it renders on a HIDDEN PAGE for the entire load.** ⚠️ **Reported by the USER on the rig, build 41, 2026-09-11: *"ok. but no progress bar."*** ⚠️ **Distinct from [I-0198]** — ✅ **that one is fixed and verified; the editor now opens** — ⚠️ **this is a SEPARATE design flaw in my own T-0500 work.** **Root cause:** ⚠️ **`progressRow_` lives INSIDE `EditorShell`**, ⚠️ **but `ScriviWindow` switched the stack to the editor ONLY in the `loadFinished` handler** (`ScriviWindow.cpp:350`) — ✅ **i.e. AFTER the load completed.** ⚠️ **So for the whole time the bar matters, the editor page is NOT the visible page**: ⚠️ **the bar shows itself on a page nobody can see, then `hideLoadProgress()` hides it the instant the page finally appears.** ⚠️ **IT COULD NEVER BE SEEN AT ANY PROJECT SIZE** — ✅ **the 1,152-scene Dumas test would have failed exactly the same way**, ⚠️ **so this was NOT "the project was too small", which is what I first assumed.** ✅ **FIX: switch to the editor page BEFORE calling `load()`.** ⚠️ **The writer then gets the window they asked for IMMEDIATELY, with an honest "Opening this project…" instead of a dead landing page.** ⚠️ **The failure path had to INVERT with it:** ⚠️ **`loadFinished(false)` previously "stayed" on landing; ✅ now the editor is already showing, so it must actively `showLanding()`.** ⚠️ **WHY NO TEST CAUGHT IT:** ⚠️ **the smokes never drive a real window stack**, ⚠️ **and "the widget was shown" is true even when its PAGE is hidden** — ✅ **only a human watching a real load could see this** (`feedback_live_pass_finds_what_suites_cannot`). | **Medium** | ✅ **SP-128** — ⚠️ **a defect in this sprint's own T-0500** | ✅ **VERIFIED 2026-09-11 (user-observed on the rig)** — ⚠️ **the only way this COULD be verified: no suite drives a real window stack.** ✅ **`the-stairs-of-tintagael` (60 scenes, ON THE NETWORK SHARE): the Manuscript View displayed, the progress bar INITIALISED, and the project loaded.** ⚠️ **The user saw ONE "The App is Not Responding → Wait" during a ~5 s load.** ⚠️ **I FIRST CALLED THIS UNEXPLAINED AND SAID A BLOCK "SHOULD NOT OCCUR ON 60 SCENES". ✅ THE USER CORRECTED ME AND WAS RIGHT.** ⚠️ **SCENE COUNT IS NOT THE ONLY DRIVER** — ✅ **[EP-039]'s rationale already says so: a per-item question costs a FULL TRAVERSAL because the core is stateless**, ⚠️ **and `findSceneMetaPath` (7 call sites) plus 28 more resolver sites each walk the whole manuscript.** ✅ **MEASURED SHAPE OF THIS PROJECT: `71` scenes, ⚠️ **`50` RELATIONSHIP EDGES** and ⚠️ **2 BOUND WORLDS**, ⚠️ **all on a network share.** ⚠️ **Every edge resolves its far endpoint through that same per-item path, and a world read crosses the mount** — ✅ **so ~5 s and one not-responding dialog is EXACTLY what EP-039 predicts for this shape.** ✅ **NOT a separate defect; NOT evidence of a leftover synchronous path.** ⚠️ **It is [EP-039]'s, and it is expected to go away when the indexes land.** |
| **I-0197** | `[Apple]` ⚠️ **Swift READS AND WRITES PROJECT-PACKAGE FILES DIRECTLY, bypassing ScriviCore — violating the architecture's central rule.** ⚠️ **Found 2026-09-10 while auditing the four unbuilt Apple surfaces** ([SP-129]). ⚠️ **CLAUDE.md is explicit: *"No backend logic is reimplemented in Swift. Swift is responsible for UI only."*** ✅ **MEASURED: `35` direct-filesystem/JSON hits across `Scrivi/App` + `Scrivi/Views` + `Scrivi/Engine`, in 11 files.** ⚠️ **They are NOT all equal, and reporting `35` as one number would be alarmist** — ✅ **they fall into THREE classes and only the first is unambiguous:** ---- ⚠️ **CLASS A — A CORE ENDPOINT EXISTS AND IS IGNORED. ✅ ONE confirmed site, and it is on a HOT PATH.** ⚠️ **`TimelineViewModel.loadImportedTimelines` (`TimelineStripView.swift:395-420`) reads `objects/imported-timelines/` with `FileManager.contentsOfDirectory`, then `Data(contentsOf:)` + `JSONDecoder` PER FILE** — ⚠️ **while `scrivi_list_imported_timelines` EXISTS, is bound at `ScriviEngine:924`, and is what LINUX calls.** ⚠️ **This sits on the timeline load path measured at `251 s` in [I-0196]**, ✅ **so it is a PERFORMANCE defect as well as an architectural one.** ✅ **Owned by [T-0502].** ---- ⚠️ **CLASS B — NO CORE ENDPOINT EXISTS, so Swift had nowhere else to go.** ⚠️ **`InspectorLayoutStore` READS AND WRITES `inspector-layout.json` INSIDE THE PROJECT PACKAGE** (`:156`, `:173`, `:304` — a `.write(to:atomic)`) — ⚠️ **and the core has ZERO inspector-layout endpoints (grep: 0).** ⚠️ **THIS IS THE MORE SERIOUS CLASS, not the lesser one:** ⚠️ **an app-layer atomic write into a package whose layout, validation and repair the CORE owns** (Doc: Project Package Structure) ⚠️ **means the core cannot validate, repair, or even SEE this file** — ⚠️ **and the External Change Repair Matrix has no row for it.** ⚠️ **It is also Git-visible project state, so it is shared between platforms that cannot read it.** ---- ⚠️ **CLASS C — EXISTENCE CHECKS ON PATHS THE CORE ALREADY RESOLVED.** ⚠️ **`ObjectCard.swift:980,984`, `ObjectImageSection.swift:106,137`, `ExistingAssetPicker.swift:130` call `FileManager.fileExists` on asset paths.** ⚠️ **`scrivi_list_assets` / `scrivi_import_asset` / `scrivi_remove_asset` exist**, ⚠️ **but none answers *"is this resolved path readable RIGHT NOW"* without opening it.** ⚠️ **These may be LEGITIMATE — a UI checking whether to draw a placeholder is not backend logic** — ✅ **but each needs a RULING, not an assumption**, ⚠️ **and on an unreachable volume a `fileExists` call is exactly the blocking-stat pattern [I-0193] cost 102 s.** ---- ✅ **WHY THIS MATTERS BEYOND TIDINESS:** ⚠️ **every bypass is a place the core's guarantees DO NOT APPLY** — no atomic-write discipline, no repair path, no external-change detection, no `soft`-mount timeout, ⚠️ **and no Linux/Windows equivalent.** ⚠️ **[I-0183] is the precedent for what an unvalidated write into a package costs.** ⚠️ **Scope is CONTAINED (11 files) — ✅ which is why it is worth closing NOW, before it spreads.** | **Medium** | ✅ **SP-130** (EP-039) | 🟠 **PARTIAL — Class C RULED 2026-09-16 (T-0508); ⛔ Classes A/B and the guard REMAIN OPEN.** ✅ **CLASS C IS CLOSED BY RULING** ([`Scrivi_Asset_Presence_Check_Ruling_v0_1.md`](../Scrivi_Asset_Presence_Check_Ruling_v0_1.md)): ✅ **R1 — a `fileExists` on a path the CORE ALREADY RESOLVED, whose only output is which view to draw, is a legitimate UI presence-check, NOT a ScriviCore bypass.** ⛔ **R1 does NOT generalise to "Swift may stat inside the package" — a stat on a path SWIFT composed is Class B.** ✅ **R2 — such a check MUST NOT run on the main actor**, ⚠️ **because these bytes may sit on a removable or network volume and the stat is UNBOUNDED.** ⚠️ **THE ISSUE'S OWN WARNING WAS RIGHT AND THE 2026-09-15 RE-MEASUREMENT WAS WRONG TO CLEAR IT:** ⚠️ **4 of 5 sites were already safe, ⛔ but `ExistingAssetPicker.swift:130` ran `fileExists` **AND a full synchronous `NSImage` decode** on the main actor, in a `LazyVStack` row builder, ⚠️ once per visible row, on bytes that live in a WORLD PACKAGE by construction** — ✅ **exactly the blocking-stat pattern [I-0193] cost 102 s, multiplied by the row count.** ⚠️ **It was cleared because its in-line comment (*"a failed load is expected, not corruption"*) answers ARCHITECTURE and was read as answering COST TOO.** ✅ **FIXED by adopting `ObjectCard.ObjectRowThumbnail`'s already-proven off-thread shape** — ⛔ **not a second mechanism.** ⚠️ **Sites 3–4 (`ObjectImageSection`) are safe for a reason the planning text did NOT give: ⛔ not "graceful degradation" but ✅ UNREACHABILITY — `preview(path)` runs only when the CORE resolved the path, so the unbounded case cannot arise.** ✅ **THE LIVE RIG PASS WAS RUN AND CLASS C IS VERIFIED (2026-09-17/18, T-0508 ✅ Verified).** ⚠️ **Still not proven by tests — no test opens the picker against an unreachable volume, and none is claimed**; ✅ **the picker popover was confirmed to open IMMEDIATELY on the real rig, and the drive-pull pass produced [I-0221] (Critical) and [I-0222], both since fixed and Verified.** ⛔ **Classes A and B remain OPEN — this Issue does NOT close on Class C alone.** ⚠️ **UPDATE 2026-09-21: CLASS B IS HALF CLOSED.** ✅ **T-0507 ([SP-141], user-Verified) built `scrivi_get_inspector_layout` / `scrivi_put_inspector_layout` and converted `InspectorLayoutStore.swift`** — ⛔ **ZERO `FileManager`, `Data(contentsOf:)` or `write(to:)` remain in it.** ⚠️ **But the SCHEMA STILL HAS TWO APP-SIDE OWNERS: Linux's `InspectorLayoutStore.cpp` is UNTOUCHED** — ✅ **[SP-142] / T-0537 retires it, now UNBLOCKED.** ⛔ **CLASS B CLOSES THERE, NOT HERE**, ⚠️ **and the Issue itself closes only when [SP-143]'s regression guard lands (AC4/AC5).** ⚠️ **CLOSED 2026-09-22 ([SP-143]/T-0510).** ✅ **ALL THREE CLASSES ARE CLOSED:** ✅ **A and C by [SP-129]/[SP-130]** (T-0508's ruling, with `ExistingAssetPicker.swift:130` a REAL defect found by writing that ruling) · ✅ **B by [SP-141] (Apple) and [SP-142] (Linux)** — ⛔ **`InspectorLayoutStore` touches the package on NEITHER platform.** ✅ **AND THE GUARD WITNESSED IT: run against `78a739f~1` — the last commit where BOTH owners existed — today's `check-package-boundary.sh` goes RED and names them** (`InspectorLayoutStore.swift:192`/`:369`, `InspectorLayoutStore.cpp:73`/`:137`), ✅ **and GREEN on the retired state with NO allow-list entry for either.** ⚠️ **IT ALSO CAUGHT [I-0241]'s disk walk at `EditorShell.cpp:2469`** — ✅ **proof it would have found that defect months earlier.** |

---

## ✅ I-0197 — closed 2026-09-22 by [SP-143] / T-0510

⚠️ **IT TOOK FIVE SPRINTS AND THREE CLASSES.** ✅ **[SP-129]/[SP-130] (Classes A and C) ·
[SP-140] (the lossless rule) · [SP-141] (core endpoints + Apple) · [SP-142] (Linux) ·
[SP-149] (the guard) · [SP-143] (the witness).**

⚠️ **THE WITNESS IS WHY THIS CLOSES HONESTLY.** ⛔ **[SP-149]'s guard was written when the code was
ALREADY CLEAN — it had only ever seen green.** ✅ **T-0510 ran it against `78a739f~1`, the last commit
where BOTH app-side owners of `scrivi.inspector-layout.v1` existed, and it went RED naming both.**
⚠️ **Without that, "we built a guard" and "the guard works" would have been the same claim.**

✅ **Two defects were found by HUMANS during this Epic that no suite could have reached** —
[I-0241] (while DESIGNING the guard) and [I-0242] (during [SP-142]'s live pass). ⚠️ **Both were
DANGLING READS: capability shipped, data custodied, no reader.** ✅ **[I-0242] remains open under
[EP-036] AC4a/AC4b.**
